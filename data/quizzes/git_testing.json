{
  "id": "git_testing",
  "title": "Pillar VI — Git Workflows, Testing & CI/CD",
  "passThreshold": 0.85,
  "sections": [
    {
      "heading": "Commits and branches: the mental model",
      "paragraphs": [
        "A git commit is a snapshot of the whole project plus a pointer to its parent commit — history is a chain of snapshots, not a pile of diffs. A branch is nothing but a movable label pointing at a commit; creating one is instant and free.",
        "The working cycle: edit files (working directory), `git add` selects changes into the staging area, `git commit` seals the staged snapshot. The staging area exists so you can commit a *coherent subset* of your current mess.",
        "Good commits are atomic — one logical change with a message explaining *why*. 'Fix login redirect for expired sessions' can be reviewed, reverted, and bisected; 'wip stuff' cannot."
      ],
      "summary": "A branch is just a lightweight pointer to a commit, so making one is instant and copies nothing.",
      "variants": [
        {
          "id": "g1",
          "question": "What actually happens when you run `git branch feature-x`?",
          "options": [
            "A new label is created pointing at the current commit — nothing is copied",
            "All uncommitted changes move to the new branch automatically",
            "A remote branch is created on origin",
            "The entire project is copied into a new directory for the branch"
          ],
          "correctAnswerIndex": 0,
          "rationale": "Branches are ~40-byte pointers into the commit graph. That's why branching in git is instant and why workflows built on many short-lived branches are practical — there is no duplication to pay for.",
          "optionExplanations": [
            "Correct — a branch is a movable pointer, nothing more.",
            "Uncommitted changes belong to the working directory, which is independent of branch pointers (they follow you until committed).",
            "Nothing touches the network until you push — git branch is purely local.",
            "That's how some pre-git systems felt; git branches share the same object store — zero copying."
          ],
          "interviewTip": "'A branch is just a pointer to a commit' is a one-sentence answer that instantly signals you understand git's model rather than memorized commands."
        },
        {
          "id": "g1-alt",
          "question": "You're on `main` with two files edited but not yet committed. You run `git checkout -b hotfix` and then `git commit -am 'patch'`. Where did that new commit land, and what happened to `main`?",
          "options": [
            "The commit landed on `main`, and `hotfix` is an empty copy waiting for its first change",
            "The commit landed on `hotfix`; `main` still points at the commit it did before, now one behind",
            "Both `main` and `hotfix` advanced to the new commit, since they started from the same place",
            "The edits were lost during checkout because they weren't committed before switching branches"
          ],
          "correctAnswerIndex": 1,
          "rationale": "`checkout -b hotfix` created a label at the same commit as `main` and moved HEAD onto it, carrying the uncommitted edits along. The commit advances only the branch HEAD points at — `hotfix` — while `main` stays frozen at the older commit. Branches diverge precisely because a commit moves one pointer, not all of them.",
          "optionExplanations": [
            "HEAD moved to `hotfix` on creation, so the commit lands there, not on `main` — and it isn't empty, it carried the working-directory edits.",
            "Correct — the commit only advances the branch HEAD is on (`hotfix`); `main` is left one commit behind.",
            "A commit advances a single pointer — the one HEAD references — never two branches at once.",
            "Uncommitted edits follow you across a checkout when there's no conflict; they were never lost."
          ],
          "interviewTip": "Say it as 'a commit only moves the branch HEAD is currently on' — that one line explains divergence, fast-forwards, and why other branches don't magically update."
        },
        {
          "id": "g1-c",
          "question": "`main` and `experiment` both point at commit `abc123`. You run `git branch -d experiment`. What did git actually remove?",
          "options": [
            "Commit `abc123` and its snapshot, since a branch owns the commits made on it",
            "The working-directory files that were created while `experiment` was checked out",
            "Only the tiny `experiment` label itself — `abc123` and every ancestor remain fully reachable through `main`",
            "The reflog entries for `experiment`, making the deletion impossible to undo"
          ],
          "correctAnswerIndex": 2,
          "rationale": "Branches don't own commits — commits live in the shared object store and a branch is merely a ~40-byte pointer into that graph. Deleting `experiment` deletes the pointer and nothing else; since `main` still references `abc123`, the history is untouched. That's why `-d` is instant and why git only warns (`-D`) when deletion would leave commits unreachable.",
          "optionExplanations": [
            "No branch owns a commit — commits belong to the object store, and `abc123` is still referenced by `main`.",
            "The working directory is independent of branch pointers; deleting a label never touches your files.",
            "Correct — branch deletion removes a pointer, not history; everything stays reachable via `main`.",
            "Reflogs are per-reference safety nets and the commits persist in the object store — even 'lost' work is typically recoverable."
          ],
          "interviewTip": "'Deleting a branch deletes a pointer, not history' is the mirror image of 'creating a branch copies nothing' — pairing the two shows you truly hold the graph model, not just commands."
        },
        {
          "id": "g1-d",
          "question": "You run `git switch -c release/v2` from the tip of `main`, then immediately run `git log` on the new branch. It shows the project's entire history. Where is that history stored for `release/v2`?",
          "options": [
            "Git copied the commit history into a per-branch log file at creation time",
            "Nowhere new — `release/v2` points at the same commit as `main`, and `git log` simply walks the parent chain backwards from that one pointer",
            "In the staging area, which snapshots the full history whenever a branch is created",
            "On the remote — `git log` queries origin to reconstruct the new branch's history"
          ],
          "correctAnswerIndex": 1,
          "rationale": "History in git is the chain of parent pointers hanging off whatever commit a ref names. A new branch adds exactly one label; `git log` starts at that commit and follows parents backwards, so the 'branch history' is just the reachable ancestry — shared, not duplicated. This is the structural reason branching is free.",
          "optionExplanations": [
            "There is no per-branch history file — history is derived by walking the commit graph, never stored per label.",
            "Correct — one new pointer, zero new data; log is a graph traversal from wherever the ref points.",
            "The staging area holds the next commit's proposed content — it has nothing to do with history or branch creation.",
            "`git log` is fully local; the entire graph already lives in your clone's object store."
          ],
          "interviewTip": "If asked 'why is branching cheap in git?', answer structurally: 'a branch is one pointer, and history is just the ancestry walk from it — nothing is copied.' Mechanism beats adjectives."
        }
      ]
    },
    {
      "heading": "Merge vs rebase — and the golden rule",
      "paragraphs": [
        "`merge` ties two histories together with a merge commit — truthful, but busy repos accumulate a criss-cross graph. `rebase` replays your commits on top of the target branch, yielding a straight line — cleaner, but the replayed commits are *new* commits with new hashes.",
        "That rewrite is why the golden rule exists: never rebase commits that others may have based work on (i.e., anything pushed to a shared branch). Rebase your local feature branch freely; rebase shared history and every collaborator's tree diverges from yours.",
        "A very common team workflow: rebase your feature branch onto main to stay current while developing, then merge (often squash-merge) into main via pull request."
      ],
      "summary": "Rebase your own private branches for a tidy history, but never rebase shared history that others build on.",
      "variants": [
        {
          "id": "g2",
          "question": "A teammate rebased the shared `develop` branch and force-pushed it. Everyone's local `develop` now 'has diverged'. What did the rebase do that caused this?",
          "options": [
            "Nothing — divergence warnings are cosmetic and safe to ignore",
            "It deleted everyone's commits from the server permanently",
            "It rewrote the branch's commits as new commits with new hashes, so every clone's history no longer matches the remote's",
            "It merged develop into every open feature branch"
          ],
          "correctAnswerIndex": 2,
          "rationale": "Rebase creates replacement commits; force-push made the remote point at the rewritten line. Every local clone still holds the old line, so git correctly reports divergence — and anyone who naively merges the two lines resurrects zombie duplicate commits. This is precisely why shared branches are rebase-forbidden.",
          "optionExplanations": [
            "Ignoring it and pulling merges the old and new lines together — the graph doubles its commits and the mess compounds.",
            "The old commits still exist in clones and reflogs — the damage is divergence and duplication risk, not immediate deletion.",
            "Correct — rewritten hashes + force-push = every collaborator's history contradicts the remote.",
            "Rebase touches one branch's history; it never fans out into other branches."
          ],
          "interviewTip": "When asked merge vs rebase, don't pick a side — state the policy: 'rebase private history for cleanliness, merge shared history for safety, never force-push shared branches.'"
        },
        {
          "id": "g2-alt",
          "question": "Your feature branch is 6 commits ahead of `main`, all still local and unpushed. `main` moved ahead with 3 new commits. You want your feature to sit cleanly on top of the latest `main` with no merge commit. Which is safe and appropriate here — and why?",
          "options": [
            "Nothing is safe here; any history operation on a branch behind `main` risks corrupting the shared repo",
            "`git rebase main` but only after force-pushing the feature branch first so the remote is authoritative",
            "`git merge main` into the feature branch, because merging is always the safer default",
            "`git rebase main` on your feature branch, because the commits are private and unshared, so rewriting their hashes harms no one"
          ],
          "correctAnswerIndex": 3,
          "rationale": "The golden rule forbids rebasing *shared* history — commits others may have built on. These 6 commits are local and unpushed, so nobody else has them; rewriting their hashes onto the new `main` tip is exactly the sanctioned use of rebase, giving the straight-line history you asked for with zero merge commit.",
          "optionExplanations": [
            "The golden rule only restricts *shared* history; local unpushed commits are the textbook case where rebase is encouraged.",
            "Force-pushing before rebasing is backwards and pointless here — the branch is unshared, so there's nothing on the remote to force over.",
            "Merging is safe but produces the merge commit you explicitly wanted to avoid; 'always safer' ignores that rebasing private history is perfectly safe.",
            "Correct — private, unpushed commits are yours alone to rewrite; rebase gives the clean linear result the golden rule permits."
          ],
          "interviewTip": "The trap is 'rebase = dangerous'. Reframe it as 'rebase = dangerous only on shared history' — for local, unpushed work it's the recommended way to stay current."
        },
        {
          "id": "g2-c",
          "question": "`feat/search` is pushed to origin, but you are its only author and nobody has based work on it. After `git rebase main`, your `git push` is rejected as 'non-fast-forward'. Why, and what's the accepted move?",
          "options": [
            "The rebase minted new hashes, so your local line no longer descends from the remote tip; on a single-author feature branch, `git push --force-with-lease` is the accepted way to publish the rewrite",
            "The rejection means the rebase corrupted the branch — reset to `origin/feat/search` and redo the work on top of it",
            "Run `git pull` first to merge the remote copy back in; merging is always preferable to any form of force-push",
            "Rebasing any pushed branch violates the golden rule outright, so the only fix is to undo the rebase entirely"
          ],
          "correctAnswerIndex": 0,
          "rationale": "Rebase replaced your commits with new-hash equivalents, so the remote tip is no longer an ancestor of your local tip — git correctly refuses a plain push. The golden rule targets history *others have built on*; a personal feature branch nobody consumes is fair game, and `--force-with-lease` publishes the rewrite while refusing to clobber the remote if someone pushed there in the meantime.",
          "optionExplanations": [
            "Correct — rewritten hashes make the push non-fast-forward by design; `--force-with-lease` is the safety-checked way to update your own feature branch.",
            "Nothing is corrupted — the rejection is git's expected response to rewritten history, not an error state to recover from.",
            "Pulling would merge the pre-rebase and post-rebase lines, duplicating every commit — the classic self-inflicted zombie graph.",
            "The golden rule is about commits others may have based work on; 'pushed' alone doesn't make history shared if you're the sole consumer."
          ],
          "interviewTip": "Name `--force-with-lease` unprompted and contrast it with bare `--force` — 'it aborts if the remote moved since my last fetch' shows you force-push with a seatbelt, not a hammer."
        },
        {
          "id": "g2-d",
          "question": "An upstream maintainer rebased and force-pushed `release`. A teammate ran a default `git pull` on their local `release`, and `git log` now shows nearly every commit twice under different hashes. What produced the duplicates?",
          "options": [
            "A network interruption during the pull re-downloaded the same commits a second time",
            "Force-push uploads a fresh copy of each commit alongside the originals on the server",
            "The teammate's reflog leaked into branch history when the pull replayed it",
            "The default pull merged the old pre-rebase line with the rewritten line — the replayed commits carry new hashes, so git treats each original/rewrite pair as two distinct commits"
          ],
          "correctAnswerIndex": 3,
          "rationale": "Rebase produces replacement commits with identical changes but new hashes. A default `git pull` is fetch + merge, so it stitched the teammate's stale line to the rewritten remote line — and since git identifies commits by hash, every original/replacement pair now coexists in the graph. Recovery is `git reset --hard origin/release` (after saving local work); prevention is never rewriting shared branches.",
          "optionExplanations": [
            "Fetch is idempotent over content — objects are addressed by hash, so a retried download can never duplicate history.",
            "Force-push moves the branch ref to the rewritten line; it doesn't store commits 'alongside' anything.",
            "Reflogs are local, per-ref journals — they are never woven into a branch's commit history by a pull.",
            "Correct — merge of the stale and rewritten lines plus hash-based identity equals every commit appearing twice."
          ],
          "interviewTip": "Walking through 'what does the teammate see after the force-push, and why does their pull make it worse?' is the strongest way to justify the golden rule — you're describing the blast radius, not reciting a slogan."
        }
      ]
    },
    {
      "heading": "Unit testing with Vitest",
      "paragraphs": [
        "A unit test isolates one piece of logic: arrange inputs, act, assert on the output. Vitest is the Vite-native runner — `describe` groups, `it` defines cases, `expect(result).toBe(expected)` asserts. Fast enough to run on every save.",
        "Good tests assert observable behavior ('given an expired token, isValid returns false'), not implementation details ('it calls parseToken once'). Behavior-based tests survive refactors; implementation-based tests break when the code improves.",
        "Determinism is what keeps a suite trusted: tests touching real time, randomness, or network are flaky by construction. Inject or mock those boundaries (`vi.useFakeTimers()`, `vi.mock(...)`) so the same input always yields the same result."
      ],
      "summary": "Test what your code does, not how it does it, and pin down time, randomness, and network so results stay reliable.",
      "variants": [
        {
          "id": "g3",
          "question": "A test for 'trial expires after 14 days' calls the real `Date.now()` and started failing this week — the fixture data aged past the window. What's the durable fix?",
          "options": [
            "Control time in the test (fake timers / inject a clock), making the test deterministic regardless of the real date",
            "Widen the assertion to accept both outcomes",
            "Delete the test since expiry logic is simple",
            "Regenerate the fixture data every couple of weeks"
          ],
          "correctAnswerIndex": 0,
          "rationale": "The test depends on wall-clock time — a hidden input that changes daily. Faking time (`vi.useFakeTimers()` + `vi.setSystemTime(...)`) or injecting a clock pins that input, so the test verifies the 14-day rule forever, not just for two weeks after someone refreshes fixtures.",
          "optionExplanations": [
            "Correct — remove nondeterminism at the source; time becomes a controlled input.",
            "A test that accepts both outcomes asserts nothing — it's a green light wired to stay green.",
            "Simple logic with real dates is exactly where silent off-by-one expiry bugs live; the test earns its keep.",
            "Scheduled human maintenance to keep a test green is a treadmill — and it *will* be forgotten."
          ],
          "interviewTip": "'How do you deal with flaky tests?' — root-cause categories (time, randomness, network, shared state, order dependence) plus 'make the boundary injectable' is a complete, senior-sounding answer."
        },
        {
          "id": "g3-alt",
          "question": "A test asserts `describe('cart', () => { it('applies a random 5–15% loyalty discount', ...) })` by calling the real `Math.random()` and checking the total is within range. It passes ~99% of runs but fails intermittently in CI with no code change. What's the correct fix?",
          "options": [
            "Add a retry so the test re-runs up to three times before it's marked failed",
            "Mock the randomness boundary (e.g. `vi.spyOn(Math, 'random').mockReturnValue(0.5)`) so the discount is a fixed, asserted value",
            "Loosen the range check to 0–100% so the assertion can never fail",
            "Move the test to run only locally, never in CI, since CI machines behave differently"
          ],
          "correctAnswerIndex": 1,
          "rationale": "The flake is nondeterminism from real randomness — a hidden input that varies each run, occasionally landing on a boundary the assertion mishandles. Stubbing `Math.random()` to a fixed value pins the input so the same computation runs every time, turning an intermittent test into a deterministic one that actually verifies the discount formula.",
          "optionExplanations": [
            "Retries paper over flakiness — they hide a real nondeterminism bug and let genuinely broken code sneak through on a lucky re-run.",
            "Correct — control the random boundary at the source; the discount becomes a fixed, deterministic input.",
            "A 0–100% range asserts nothing about a 5–15% rule; it's a test wired to always pass.",
            "'Skip it in CI' abandons the coverage exactly where it matters most and doesn't fix the nondeterminism."
          ],
          "interviewTip": "Retrying a flaky test is a smell, not a fix. Name the nondeterministic source — here, randomness — and pin it. Interviewers listen for whether you remove the cause or mask the symptom."
        },
        {
          "id": "g3-c",
          "question": "A Vitest suite for a currency formatter calls the real `fetch('https://api.rates.example/latest')` to get exchange rates. It fails whenever the API is slow, rate-limits CI, or the rates drift from the asserted values. What's the durable fix?",
          "options": [
            "Raise the test timeout to 30 seconds so slow API responses stop failing the suite",
            "Loosen the assertion to 'result is a non-empty string', since live rates can't be predicted",
            "Schedule the suite to run overnight when the API is under less load",
            "Mock the network boundary (`vi.mock` / a stubbed `fetch`) so the formatter runs against fixed response data and produces the same output every run"
          ],
          "correctAnswerIndex": 3,
          "rationale": "The test smuggles in three hidden inputs — latency, availability, and live data — none of which the formatter logic controls. Stubbing the fetch pins the response, so the unit test verifies formatting deterministically; whether the real API honors its contract is a separate, explicitly-marked integration concern, not something every unit run should gamble on.",
          "optionExplanations": [
            "A longer timeout addresses one of three failure modes and makes the slow case slower — rate limits and drifting data still break the suite.",
            "'Is a string' passes for garbage output — the assertion no longer verifies formatting at all.",
            "Rescheduling dodges load but not outages or changing rates, and it decouples feedback from the push that caused a failure.",
            "Correct — a stubbed response makes the network a controlled input; the unit test becomes deterministic and fast."
          ],
          "interviewTip": "Draw the line crisply: unit tests mock the network to test *your* logic; a thin, separate integration test checks the API contract. Conflating the two is how suites become slow and distrusted."
        },
        {
          "id": "g3-d",
          "question": "`saveDraft` debounces calls by 300ms. Its test invokes it, then does `await new Promise(r => setTimeout(r, 350))` before asserting the save fired once. It adds real wall-time to every run and still flakes on loaded CI machines. What's the right approach?",
          "options": [
            "`vi.useFakeTimers()` then `vi.advanceTimersByTime(300)` — the test drives virtual time explicitly, runs instantly, and can't lose a race with the scheduler",
            "Raise the real wait to 1000ms so even a heavily loaded CI machine has enough slack",
            "Change the app's debounce to 0ms so the test doesn't need to wait at all",
            "Assert the save fired 'zero or one times' so scheduler jitter can never fail the test"
          ],
          "correctAnswerIndex": 0,
          "rationale": "Sleeping real time makes the test both slow and probabilistic — 350ms is a bet that the event loop stays under 50ms of jitter, a bet loaded CI machines lose. Fake timers replace the clock with one the test owns: `advanceTimersByTime(300)` fires the debounce synchronously and exactly, so the test is instant and cannot race.",
          "optionExplanations": [
            "Correct — time becomes a controlled input the test advances deliberately; no waiting, no race.",
            "A bigger sleep shrinks the flake probability while growing the runtime — it's still a wager on machine load, just a slower one.",
            "Rewriting production behavior to suit a test inverts the relationship — and a 0ms debounce is no longer a debounce.",
            "'Zero or one' passes when the save never fires — the assertion is wired to stay green regardless of correctness."
          ],
          "interviewTip": "Real sleeps in unit tests fail twice: slow when they pass, flaky when the machine is busy. 'Fake timers make time an input I advance explicitly' is the one-line senior answer."
        }
      ]
    },
    {
      "heading": "End-to-end testing with Playwright",
      "paragraphs": [
        "Unit tests check parts; end-to-end tests check the assembled machine: Playwright drives a real browser through real flows — navigate, click, type, assert on what a user would see.",
        "Prefer user-facing selectors — `getByRole('button', { name: 'Save' })`, `getByLabel('Email')` — over CSS/XPath tied to DOM structure. They survive refactors and double as accessibility checks: if getByRole can't find your button, neither can a screen reader.",
        "Playwright's assertions auto-wait and retry (`await expect(locator).toBeVisible()`), which eliminates the sleep-based flakiness older tools suffered. The testing pyramid still applies: many fast unit tests, fewer integration tests, a thin crust of E2E on the critical paths."
      ],
      "summary": "Find elements the way a user would (by role and label) and wait on real conditions instead of guessing with sleeps.",
      "variants": [
        {
          "id": "g4",
          "question": "Why does Playwright's own documentation push `getByRole('button', { name: 'Submit' })` over `page.locator('#app > div.form > button:nth-child(3)')`?",
          "options": [
            "nth-child is not supported inside locator()",
            "Role selectors execute significantly faster in the browser",
            "Role/name selectors track what users (and assistive tech) perceive, so they survive DOM refactors and implicitly verify accessibility",
            "CSS selectors are deprecated in modern Playwright"
          ],
          "correctAnswerIndex": 2,
          "rationale": "The structural selector encodes today's DOM shape — one wrapper div added and the test lies about the app being broken. The role selector encodes intent ('the button a user knows as Submit'), which changes only when the actual UX changes — and it fails if the button loses its accessible name, catching a11y regressions for free.",
          "optionExplanations": [
            "nth-child works fine in locator() — which is part of the problem: it lets you write brittle tests.",
            "Selector speed differences are negligible next to browser and network time.",
            "Correct — resilience to refactors plus built-in accessibility pressure.",
            "CSS selectors remain fully supported; the guidance is preference, not deprecation."
          ],
          "interviewTip": "'How do you keep E2E suites from becoming flaky and hated?' — answer: user-facing selectors, auto-waiting assertions, test isolation, and keeping E2E thin per the pyramid. Four beats, well rehearsed."
        },
        {
          "id": "g4-alt",
          "question": "A test does `await page.waitForTimeout(3000)` then clicks a 'Checkout' button that appears after an async cart load. It passes locally but fails ~10% of the time in CI, where the machine is slower. What's the idiomatic Playwright fix?",
          "options": [
            "Wrap the click in a try/catch and retry it in a loop until it succeeds",
            "Disable the test in CI and run it only on the faster local machine",
            "Bump the timeout to `waitForTimeout(8000)` to give the slow CI box more headroom",
            "Replace the fixed sleep with an auto-waiting assertion/locator like `await page.getByRole('button', { name: 'Checkout' }).click()`, which retries until the element is actionable"
          ],
          "correctAnswerIndex": 3,
          "rationale": "A hardcoded `waitForTimeout` guesses at how long the async load takes — too short and it flakes on slow CI, too long and it wastes time. Playwright's locators and web-first assertions auto-wait and retry until the element is present and actionable, so the test synchronizes on the actual condition instead of a fragile fixed delay. That's the mechanism that kills sleep-based flakiness.",
          "optionExplanations": [
            "A manual retry loop reinvents what Playwright's auto-waiting already does, worse and more verbosely.",
            "Skipping in CI abandons coverage on the exact environment where the race actually surfaces.",
            "A bigger fixed sleep just moves the flake threshold and slows every run; it still guesses instead of waiting on the real condition.",
            "Correct — auto-waiting locators retry until the button is actually clickable, eliminating the timing guess entirely."
          ],
          "interviewTip": "Any `waitForTimeout` / `sleep` in an E2E test is a red flag. The senior answer: 'wait on a condition, not a duration' — Playwright's auto-waiting assertions do this for you."
        },
        {
          "id": "g4-c",
          "question": "During a Tailwind migration a designer renames utility classes, and the E2E test using `page.locator('div.account-panel .btn-danger')` for the 'Close account' flow goes red — while the feature works perfectly in a real browser. What should replace the selector?",
          "options": [
            "A longer XPath anchored on the panel's exact position in the DOM tree, which is more precise than class names",
            "`getByRole('button', { name: 'Close account' })` — it targets what the user perceives, so it survives styling refactors and fails only when the actual UX or its accessible name breaks",
            "A `data-style` attribute mirroring the old class names so the existing selector keeps matching",
            "A pixel screenshot comparison of the button, since visual diffs can't be broken by class renames"
          ],
          "correctAnswerIndex": 1,
          "rationale": "The test just produced a false alarm: it failed while the product worked, because it encoded a styling detail users never see. A role + accessible-name locator encodes intent — 'the button a user knows as Close account' — so cosmetic refactors pass silently, and a failure now means something real: the control vanished or lost its accessible name, which is itself an a11y regression worth catching.",
          "optionExplanations": [
            "Positional XPath is *more* brittle, not less — one wrapper div and it breaks again, still without telling you anything about the user experience.",
            "Correct — selecting by role and name couples the test to user-perceived behavior instead of the styling layer.",
            "Freezing dead class names into a shadow attribute preserves the coupling problem and adds maintenance debt to every future restyle.",
            "Pixel diffs break on precisely this kind of change — restyled buttons render differently — and they can't click through a flow."
          ],
          "interviewTip": "Frame it as failure semantics: 'I want my E2E test to fail if and only if a user's experience broke.' Structural selectors fail on refactors; role/name selectors fail on real regressions."
        },
        {
          "id": "g4-d",
          "question": "After clicking 'Apply coupon', a test immediately reads the price: `const text = await page.locator('.total').textContent(); expect(text).toBe('$36.00');`. It fails whenever the recalculation request is slow. Why, and what's the idiomatic fix?",
          "options": [
            "`textContent()` is deprecated; switching to `innerText()` makes Playwright wait for network idle first",
            "Insert `await page.waitForTimeout(2000)` before the read so the request has time to finish",
            "The one-shot read races the async update — a web-first assertion like `await expect(page.locator('.total')).toHaveText('$36.00')` re-queries and retries until it passes or times out",
            "Wrap the `expect` in a manual polling loop that re-reads the text every 100ms until it matches"
          ],
          "correctAnswerIndex": 2,
          "rationale": "The moment you extract a value with `textContent()` and hand it to a plain `expect`, you've left Playwright's retry machinery — the assertion checks a stale snapshot taken before the recalculation landed. `expect(locator).toHaveText(...)` keeps the locator live: it re-reads the DOM on each retry until the condition holds, synchronizing on the actual outcome rather than a guess about timing.",
          "optionExplanations": [
            "`textContent()` isn't deprecated and `innerText()` has no network-idle magic — the race is in the one-shot read, not the method name.",
            "A fixed sleep trades one race for a slower one — still a duration-based guess that flakes when the request exceeds it.",
            "Correct — web-first assertions operate on the locator, retrying the read itself until the expected state appears.",
            "Hand-rolled polling reimplements `toHaveText` without its timeout handling, error reporting, or trace integration."
          ],
          "interviewTip": "The subtle trap is extracting a value and asserting on it — auto-waiting lives in `expect(locator)`, not `expect(value)`. Say 'keep assertions locator-based so they stay retryable' and you've flagged a mistake most juniors ship."
        }
      ]
    },
    {
      "heading": "CI/CD: the pipeline as gatekeeper",
      "paragraphs": [
        "Continuous Integration: every push triggers an automated pipeline — install, lint, type-check, test, build. The point is shrinking the distance between introducing a bug and hearing about it from weeks to minutes.",
        "A typical GitHub Actions flow: on pull request, run the checks; merging is blocked until they're green plus a human review. Continuous Delivery/Deployment extends the green path: merged main auto-deploys to staging, then production — with prod gated manually (Delivery) or fully automatic (Deployment).",
        "Pipelines fail-fast in order of cheapness: lint before tests, unit before E2E. And a red main is a stop-the-line event — the fix or the revert outranks new work, because everyone is building on top of it."
      ],
      "summary": "Run the cheapest checks first for fast feedback, and drop everything to fix a broken `main` since the whole team builds on it.",
      "variants": [
        {
          "id": "g5",
          "question": "A PR's pipeline runs: lint → unit tests → E2E tests → build. E2E is the slowest stage. Why do lint and unit tests run first?",
          "options": [
            "Fail fast: cheap checks catch most failures in seconds, so broken pushes don't burn 20 minutes of E2E before reporting the lint error",
            "E2E tests can only run after a deployment to production",
            "Lint must pass before code will compile at all",
            "Alphabetical ordering of pipeline stages"
          ],
          "correctAnswerIndex": 0,
          "rationale": "Pipeline ordering is economics: fastest-and-most-likely-to-fail first. A typo that lint catches in 10 seconds shouldn't cost a full E2E run to discover. Same total coverage, radically better feedback latency for the common failure cases.",
          "optionExplanations": [
            "Correct — fail-fast ordering optimizes time-to-feedback, which is the entire point of CI.",
            "E2E runs against a preview or local server in CI — production deployment is not a prerequisite.",
            "Linting is style/correctness analysis, independent of compilation; unlinted code compiles fine.",
            "Stage order is authored deliberately in the workflow file; nothing is alphabetical."
          ],
          "interviewTip": "If asked to design a CI pipeline, narrate it in cost order — 'lint, type-check, unit, integration, E2E, build, deploy' — and mention branch protection on green checks. Structure is the signal."
        },
        {
          "id": "g5-alt",
          "question": "Your team's `main` branch pipeline goes red: a merged commit broke the build. A developer opens a fresh PR to add an unrelated new feature and asks to merge it 'since it's green on its own branch'. What does a healthy CI/CD culture do first?",
          "options": [
            "Merge the new feature — it's green in isolation, and unrelated to the breakage",
            "Stop the line: fixing or reverting the broken `main` outranks new work, because everyone branches from `main` and builds on the breakage",
            "Disable the failing check on `main` so the pipeline goes green and work can continue",
            "Wait a day to see if the failure resolves itself before taking any action"
          ],
          "correctAnswerIndex": 1,
          "rationale": "A red `main` is a stop-the-line event. Everyone cuts new branches from `main` and merges back into it, so a broken `main` poisons every downstream branch and hides which failures are new. Restoring green — by fixing forward or reverting the offending commit — takes priority over any new feature, however clean that feature looks in isolation.",
          "optionExplanations": [
            "Merging onto a broken `main` stacks new work atop the breakage and muddies which failures are the new PR's fault.",
            "Correct — a red shared branch is stop-the-line; fix or revert before new work, because the whole team builds on `main`.",
            "Silencing the check hides the breakage instead of resolving it — the bug ships and trust in the pipeline erodes.",
            "Build failures don't self-heal; waiting just extends the window in which everyone inherits a broken base."
          ],
          "interviewTip": "'Stop the line' is the phrase interviewers want. A red shared branch blocks the whole team, so its fix or revert always outranks new feature work — say that explicitly."
        },
        {
          "id": "g5-c",
          "question": "A team inherited a pipeline that runs stages in this order: E2E suite (25 min) → build → unit tests → type-check → lint. Developers routinely wait half an hour to learn they missed a semicolon. What's the highest-leverage restructure?",
          "options": [
            "Cache the E2E browser binaries so the 25-minute stage starts a little sooner",
            "Drop the lint and type-check stages entirely — a passing E2E suite already proves the app works",
            "Collapse everything into one giant job so at least all failures arrive in a single report",
            "Invert to cost order — lint and type-check in seconds, then unit tests, then build, then E2E — so the cheap checks that catch most failures report first"
          ],
          "correctAnswerIndex": 3,
          "rationale": "The pipeline's coverage is fine; its *ordering* is economically backwards. Most failures are cheap ones — syntax, types, broken units — and this layout taxes every one of them with a 25-minute E2E toll before reporting. Reordering costs nothing, keeps identical total coverage, and collapses time-to-feedback for the common case from ~30 minutes to seconds.",
          "optionExplanations": [
            "Shaving startup time off the wrong first stage still leaves a 20-plus-minute wait to hear about a lint error.",
            "E2E proves happy paths render, not that types are sound or edge-case units behave — deleting cheap checks trades feedback for silence.",
            "One mega-job still runs the slow work before reporting and gives up the clarity of per-stage failure signals.",
            "Correct — fail-fast ordering: same checks, same rigor, radically faster feedback on the failures that happen most often."
          ],
          "interviewTip": "Pitch pipeline design as an economics problem: 'order stages by cost times likelihood of failure.' That one framing shows you optimize for time-to-feedback, which is the entire point of CI."
        },
        {
          "id": "g5-d",
          "question": "One E2E test on `main` fails roughly one run in five. The team's habit is clicking 're-run jobs' until it goes green, and a dev proposes marking the whole E2E suite non-required 'since it's basically noise'. What does a healthy CI culture do instead?",
          "options": [
            "Treat the flake as a defect: root-cause or quarantine that one test with a tracked ticket, and keep the suite required — a check people re-run until green has stopped gating anything",
            "Accept the proposal — non-required checks still show red, so anyone who cares can inspect them",
            "Configure the workflow to auto-retry every E2E job three times so humans stop clicking the button",
            "Move the E2E suite to a nightly schedule where its flakiness can't block anyone's merge"
          ],
          "correctAnswerIndex": 0,
          "rationale": "A CI gate is only worth what the team believes it means. 'Re-run until green' trains everyone to treat red as noise, which is exactly the state in which a real regression sails through on its second attempt. The fix is surgical: diagnose or quarantine the single flaky test under a tracked issue so the remaining suite stays trustworthy and required — preserving the gate instead of dismantling it.",
          "optionExplanations": [
            "Correct — isolate the one bad test, keep the gate meaningful; flakiness is a bug in the test system and gets triaged like one.",
            "Optional checks are ignored checks — within weeks the suite is decoration and its regressions ship unnoticed.",
            "Automating the re-runs institutionalizes the coin flip: a genuine one-in-five failure now needs only one lucky retry to merge.",
            "Nightly runs decouple failures from the commits that caused them, turning actionable PR feedback into next-morning archaeology."
          ],
          "interviewTip": "The phrase to land is 'trust in the pipeline is the asset'. Quarantine-with-a-ticket shows you can keep the gate strict without letting one bad test hold the whole team hostage."
        }
      ]
    }
  ]
}

{
  "id": "node",
  "title": "Pillar III — Node.js, APIs & Async Control Flow",
  "passThreshold": 0.85,
  "sections": [
    {
      "heading": "The event loop: one thread, no waiting",
      "paragraphs": [
        "Node runs your JavaScript on a single thread. It handles thousands of simultaneous connections anyway because I/O (disk, network, database) doesn't happen on that thread — Node hands the operation to the system, registers a callback, and moves on.",
        "The event loop is the dispatcher: when the call stack is empty, it pulls completed callbacks from the queues and runs them. Microtasks (resolved promises) run before macrotasks (timers, I/O callbacks) — which is why `Promise.resolve().then(...)` beats `setTimeout(..., 0)`.",
        "The corollary: CPU-heavy synchronous code is poison. While your JavaScript runs, nothing else runs — every pending request waits behind your loop."
      ],
      "variants": [
        {
          "id": "d1",
          "question": "An Express server handles one route that synchronously computes a huge report for 3 seconds. During those 3 seconds, what happens to other incoming requests?",
          "options": [
            "They queue and wait — the single JS thread is busy, so the event loop can't dispatch anything until the computation finishes",
            "Node automatically rejects them with 503 responses",
            "They're handled fine because Express requests are isolated",
            "Node spawns a thread per request, so they proceed normally"
          ],
          "correctAnswerIndex": 0,
          "rationale": "One thread runs all JavaScript. A 3-second synchronous computation blocks the event loop, so every other request — even trivial health checks — waits the full 3 seconds. Fixes: chunk the work, move it to a worker thread, or push it to a job queue.",
          "optionExplanations": [
            "Correct — 'blocking the event loop' means exactly this: everything stalls behind synchronous work.",
            "Node sends nothing automatically; connections just sit in the queue, timing out at the client's discretion.",
            "Express routes share the same single thread; isolation is logical, not computational.",
            "That's the Java/traditional model. Node's whole design is one JS thread plus non-blocking I/O — no thread per request."
          ],
          "interviewTip": "'How does Node handle concurrency with one thread?' — answer with the split: I/O concurrency comes free from the event loop; CPU concurrency requires worker threads or separate processes."
        },
        {
          "id": "d1-alt",
          "question": "Inside a request handler you write `Promise.resolve().then(() => log('A')); setTimeout(() => log('B'), 0); log('C');`. In what order do A, B, and C print?",
          "options": [
            "A, B, C — queued callbacks always run before synchronous code",
            "C, A, B — synchronous code runs first, then the microtask (resolved promise), then the macrotask (timer)",
            "C, B, A — the timer was registered before the promise resolved, so B wins",
            "A, C, B — the promise callback is hoisted ahead of the synchronous log"
          ],
          "correctAnswerIndex": 1,
          "rationale": "`log('C')` is synchronous and runs immediately while the stack is unwinding. When the stack empties, the event loop drains the microtask queue (the resolved promise's `.then`) before any macrotask, so A prints next. `setTimeout` schedules a macrotask, which runs last — giving C, A, B.",
          "optionExplanations": [
            "Synchronous code on the current stack always runs before any queued callback — the queues are only serviced once the stack is empty.",
            "Correct — synchronous first (C), then microtasks (A), then macrotasks (B). Microtasks always beat timers.",
            "Registration order across queue types is irrelevant: microtasks are drained fully before the loop touches the timer queue, so A precedes B regardless.",
            "Nothing is 'hoisted' — the `.then` callback cannot run until the current synchronous stack (including `log('C')`) completes."
          ],
          "interviewTip": "The reliable ordering rule to state: 'current synchronous stack → drain all microtasks → one macrotask → repeat.' `Promise` callbacks are microtasks; `setTimeout`/`setImmediate`/I/O are macrotasks."
        },
        {
          "id": "d1-c",
          "question": "Under a burst of logins, your `/healthz` endpoint starts timing out even though it only returns `{ ok: true }`. Profiling shows each login calls `bcrypt.hashSync` (~80ms of pure CPU). Why is the health check affected, and what's the right fix?",
          "options": [
            "The health route shares the login route's connection pool; raising the pool size restores it",
            "Each 80ms synchronous hash monopolizes the one JS thread, and queued logins stack those blocks back-to-back — the health check's callback can't be dispatched until the loop is free; switch to the async `bcrypt.hash` (or offload to a worker thread)",
            "HTTP keep-alive is pinning the health check's socket behind login sockets; disabling keep-alive fixes it",
            "Node prioritizes routes by registration order, so registering `/healthz` first restores it"
          ],
          "correctAnswerIndex": 1,
          "rationale": "80ms of synchronous CPU doesn't sound like much, but under a login burst those blocks queue back-to-back on the single JS thread, and every other callback — including a trivial health check — waits behind the entire backlog. The async `bcrypt.hash` runs the hashing off the JS thread (libuv threadpool), keeping the loop free to dispatch other work; worker threads are the general answer for CPU-bound code.",
          "optionExplanations": [
            "There is no connection pool between routes — the contention is on the JS thread itself, and no pool setting frees it.",
            "Correct — the symptom of event-loop blocking is unrelated endpoints getting slow; the fix is moving CPU work off the JS thread, not tuning the fast route.",
            "Keep-alive reuses sockets; it doesn't serialize unrelated requests. The stall happens after the request arrives, in the blocked loop.",
            "Route registration order affects matching, not scheduling — a registered-first route still can't run while the thread is executing someone else's hash."
          ],
          "interviewTip": "Latency spikes on a trivially cheap endpoint are the textbook symptom of event-loop blocking. Diagnosing from the victim endpoint rather than the guilty one is exactly the reasoning interviewers want to hear."
        },
        {
          "id": "d1-d",
          "question": "A handler runs: `setTimeout(() => log('T'), 0); (async () => { log('S'); await null; log('W'); })(); log('E');`. What prints, in order?",
          "options": [
            "S, W, E, T — an async function runs to completion before control returns to the caller",
            "E, S, W, T — async function bodies are deferred to the microtask queue from the start",
            "S, E, W, T — the async body runs synchronously until the `await`, `E` finishes the stack, the post-`await` continuation runs as a microtask, and the timer macrotask goes last",
            "S, E, T, W — the timer was queued before the `await`, so it fires before the continuation"
          ],
          "correctAnswerIndex": 2,
          "rationale": "Calling an async function executes its body synchronously up to the first `await` — so S prints during the call. `await null` suspends the function and control returns to the caller, which prints E. When the stack empties, the continuation (W) runs from the microtask queue before the loop services the timer queue, so T is last: S, E, W, T.",
          "optionExplanations": [
            "An async function yields at its first `await`; it does not run to completion before returning — that's the whole point of the suspension.",
            "The body is not deferred: everything before the first `await` executes synchronously during the call, so S beats E.",
            "Correct — synchronous prefix (S), rest of the stack (E), microtask continuation (W), then the macrotask timer (T).",
            "Queue registration time doesn't matter across queue types: microtasks are drained completely before any timer runs, so W always precedes T."
          ],
          "interviewTip": "The nuance interviewers probe: `await` splits an async function into a synchronous prefix and a microtask continuation. Say 'runs synchronously until the first await' and you've disarmed the whole question family."
        }
      ]
    },
    {
      "heading": "Promises and async/await",
      "paragraphs": [
        "A promise is a placeholder for a value that isn't ready: pending, then either fulfilled or rejected. `async/await` is syntax over promises — `await` pauses that async function (not the thread!) until the promise settles.",
        "The classic performance mistake is sequential awaiting of independent work: `const a = await getA(); const b = await getB();` takes the sum of both latencies. Independent operations should start together: `const [a, b] = await Promise.all([getA(), getB()]);` — now you pay only the slower one.",
        "Know the combinators: `Promise.all` fails fast on the first rejection; `Promise.allSettled` waits for every result, success or failure; `Promise.race` settles with whichever finishes first."
      ],
      "variants": [
        {
          "id": "d2",
          "question": "getUser() takes ~200ms and getOrders() takes ~300ms; they don't depend on each other. `await getUser(); await getOrders();` takes ~500ms total. What change gets it to ~300ms?",
          "options": [
            "Use process.nextTick to prioritize the calls",
            "Wrap both awaits in a try/catch block",
            "Start both promises first, then await together: `Promise.all([getUser(), getOrders()])`",
            "Convert both functions to synchronous versions"
          ],
          "correctAnswerIndex": 2,
          "rationale": "Sequential awaits serialize independent I/O. Promise.all starts both immediately and resolves when the slowest settles — total time becomes max(200, 300) ≈ 300ms instead of the 500ms sum.",
          "optionExplanations": [
            "process.nextTick reorders microtask timing by microseconds; it cannot overlap two network calls.",
            "try/catch changes error handling, not scheduling — still 500ms.",
            "Correct — concurrent start, single await, pay only the slowest.",
            "Synchronous I/O would block the entire event loop for 500ms — dramatically worse for every other request."
          ],
          "interviewTip": "Spotting 'sequential awaits on independent operations' in a code-review interview is a rehearsed scenario at many companies. Scan for it explicitly."
        },
        {
          "id": "d2-alt",
          "question": "You fire five independent inventory lookups with `const results = await Promise.all(calls);`. One of the five services is down and its promise rejects. What does `results` end up being, and what's the fix if you need every store's status regardless?",
          "options": [
            "`Promise.all` retries the failed call automatically, so `results` eventually fills in",
            "`results` is an array of promises you must await individually afterward",
            "`results` holds the four successes with `undefined` in the failed slot; no change needed",
            "The whole `await` rejects and `results` is never assigned — swap to `Promise.allSettled` to collect every outcome, success or failure"
          ],
          "correctAnswerIndex": 3,
          "rationale": "`Promise.all` fails fast: the first rejection rejects the aggregate immediately, so the `await` throws and `results` is never bound — the four successes are discarded. When you need every result whether it succeeded or failed, `Promise.allSettled` resolves to an array of `{status, value|reason}` objects, one per input.",
          "optionExplanations": [
            "`Promise.all` has no retry behavior; it only aggregates settlement — you'd build retries yourself.",
            "`Promise.all` returns a single promise resolving to an array of values, not an array of promises.",
            "`Promise.all` does not substitute `undefined` for failures; a single rejection discards the entire aggregate result.",
            "Correct — `all` rejects on first failure; `allSettled` waits for all and reports each outcome individually."
          ],
          "interviewTip": "Know the combinator table cold: `all` (fail-fast, all-or-nothing), `allSettled` (never rejects, per-item outcomes), `race` (first to settle wins), `any` (first to fulfill, ignores rejections until all fail)."
        },
        {
          "id": "d2-c",
          "question": "`for (const id of skus) { prices.push(await fetchPrice(id)); }` over 10 independent SKUs at ~120ms each takes ~1.2s. Which rewrite brings it to ~120ms, and why?",
          "options": [
            "`const prices = await Promise.all(skus.map((id) => fetchPrice(id)))` — the `map` fires all ten requests immediately, so the single await pays only the slowest call",
            "Replace the `for...of` with `skus.forEach(async (id) => ...)` — the loop body then runs in parallel and `prices` is populated right after the loop",
            "Move each iteration into `process.nextTick` so the fetches interleave between loop turns",
            "Switch to `for await (const id of skus)` — `for await` makes any loop's iterations concurrent automatically"
          ],
          "correctAnswerIndex": 0,
          "rationale": "The `await` inside the loop body means iteration N+1 can't even start its request until N's response arrives — ten sequential 120ms round-trips. Mapping to an array of promises starts all ten requests in the same tick; the one `await Promise.all` then resolves when the slowest settles, so total time collapses to roughly max instead of sum.",
          "optionExplanations": [
            "Correct — start-all-then-await-once is the pattern; the requests overlap in flight and you pay ~max(latencies) instead of their sum.",
            "`forEach` ignores the promises its async callback returns — the loop 'finishes' instantly while every fetch is still pending, and `prices` is empty (or partially filled) when you read it.",
            "`process.nextTick` shuffles microtask timing; the loop still awaits each response before issuing the next request, so it stays ~1.2s.",
            "`for await` changes how an async iterable is consumed; with the fetch call still inside the body, each iteration still awaits before the next one starts."
          ],
          "interviewTip": "'await in a loop' is the code-review flag interviewers plant deliberately. The follow-up they want: when the list is large, cap concurrency (batching or a limiter like `p-limit`) rather than firing thousands of requests at once."
        },
        {
          "id": "d2-d",
          "question": "You migrate a batch notifier from `Promise.all` to `Promise.allSettled` and keep the surrounding `try/catch` to log failures. Sends fail all week, but the `catch` block never fires and nothing is logged. What's happening?",
          "options": [
            "`allSettled` swallows errors permanently; once it's in place, failures are unobservable",
            "The `try/catch` is scoped wrong — moving it inside each promise callback restores the logging",
            "The promise `Promise.allSettled` returns never rejects — failures arrive as fulfilled `{status: 'rejected', reason}` entries in the result array, so you must inspect each outcome (e.g., filter for `status === 'rejected'`) instead of relying on `catch`",
            "`allSettled` retries failed promises until they succeed, so there are no failures left to log"
          ],
          "correctAnswerIndex": 2,
          "rationale": "`Promise.allSettled` fulfills unconditionally with one descriptor per input: `{status: 'fulfilled', value}` or `{status: 'rejected', reason}`. Since the aggregate never rejects, the `await` never throws, and a `try/catch` around it is dead code for per-item failures. Error handling must move from exception flow to data flow: iterate the results and act on the rejected entries.",
          "optionExplanations": [
            "Nothing is swallowed — every failure is right there in the result array as a `reason`; it's just delivered as data, not as a thrown exception.",
            "Relocating the `try/catch` misses the point: the design of `allSettled` is that failures are reported in the fulfillment value, not thrown anywhere.",
            "Correct — switching combinators changes the error-delivery channel from throw to data; code that only listens for throws goes silently blind.",
            "No combinator retries anything; `allSettled` simply waits for every input to settle once and reports what happened."
          ],
          "interviewTip": "A sharp follow-up you can preempt: 'When would allSettled hide bugs?' — when callers forget that error handling moved into the result array. Pair any `allSettled` with an explicit pass over the `rejected` entries."
        }
      ]
    },
    {
      "heading": "Designing REST APIs",
      "paragraphs": [
        "REST URLs name resources with nouns; HTTP methods supply the verbs. `GET /users/42/orders` reads orders for user 42. `POST /orders` creates. Verbs in URLs (`/getUser`, `/createOrder`) are the RPC habit REST replaces.",
        "Respond with codes that match what happened: `201 Created` (with a `Location` header) for creation, `204 No Content` for a successful delete, `400` for malformed input, `422` for well-formed input that fails validation, `404` for a missing resource.",
        "Version your API (`/v1/...`) from day one, and never break the contract of a shipped version — clients you don't control are relying on the shape of your responses."
      ],
      "variants": [
        {
          "id": "d3",
          "question": "Which endpoint design best follows REST conventions for 'cancel order 42'?",
          "options": [
            "POST /orders/42/cancel  (or PATCH /orders/42 with a status change)",
            "DELETE /orders/cancel/42",
            "PUT /cancel with the order id in the body",
            "GET /cancelOrder?id=42"
          ],
          "correctAnswerIndex": 0,
          "rationale": "Cancellation is a state transition on order 42, not a read and usually not a removal — the order record persists as 'cancelled'. Modeling it as a sub-action POST or a PATCH of the status field keeps the resource-oriented grammar while expressing a domain action.",
          "optionExplanations": [
            "Correct — a state transition modeled on the resource. Both forms are accepted; be ready to defend either.",
            "DELETE says the resource ceases to exist, but a cancelled order must remain queryable — plus the URL structure is scrambled.",
            "PUT means replace the resource at this URL; '/cancel' isn't a resource, and hiding the id in the body breaks addressability.",
            "A GET with side effects is the cardinal REST sin — prefetchers and proxies re-issue GETs freely and would cancel orders on their own."
          ],
          "interviewTip": "'How do you model actions that aren't CRUD?' is a designed-in follow-up. The phrase to offer: 'model the state transition, not the verb' — then mention both the sub-resource POST and the PATCH approach."
        },
        {
          "id": "d3-alt",
          "question": "A client submits the same `POST /payments` twice because the network dropped the first response. Which HTTP-method property is at stake, and which status code correctly signals a duplicate creation attempt on a resource that already exists?",
          "options": [
            "`GET` is idempotent, so the fix is to change the endpoint to `GET /payments`",
            "`POST` is not idempotent by default; guard it with a client-supplied idempotency key and return `409 Conflict` (or the original `201`) on the duplicate rather than creating a second payment",
            "`POST` is idempotent, so a second identical `POST` is always safe and returns `200`",
            "The correct response to any duplicate is `400 Bad Request` because the client sent the request twice"
          ],
          "correctAnswerIndex": 1,
          "rationale": "`POST` is neither safe nor idempotent — repeating it can create duplicate resources. The standard defense is an idempotency key the server records so a retry maps to the original operation, returning `409 Conflict` when the key collides with a different in-flight/completed create (or replaying the original `201`). This is exactly why `PUT` (idempotent) is preferred when the client controls the identifier.",
          "optionExplanations": [
            "`GET` must be safe — it cannot create a payment at all; switching the verb loses the write semantics entirely.",
            "Correct — `POST` isn't idempotent; idempotency keys plus `409`/replayed-`201` make retries safe without double-charging.",
            "`POST` is explicitly non-idempotent in the HTTP spec; assuming otherwise is what causes duplicate charges.",
            "`400` means malformed syntax; a well-formed duplicate is a conflict (`409`) or a replayed success, not a bad request."
          ],
          "interviewTip": "Memorize method properties: `GET`/`HEAD` safe + idempotent; `PUT`/`DELETE` idempotent (not safe); `POST`/`PATCH` neither. 'How do you make `POST` retry-safe?' → idempotency keys."
        },
        {
          "id": "d3-c",
          "question": "Your API needs 'deactivate account 7' — the account must remain readable afterward for audit and possible reactivation. Which design best fits REST conventions?",
          "options": [
            "`DELETE /accounts/7` — deactivation is a removal, and clients can recreate the account later",
            "`GET /accounts/deactivate/7` — reads are cache-friendly and the URL is self-describing",
            "`PUT /deactivate` with `{ \"accountId\": 7 }` in the body",
            "`POST /accounts/7/deactivation` (or `PATCH /accounts/7` setting `status: \"inactive\"`) — a state transition expressed on the resource itself"
          ],
          "correctAnswerIndex": 3,
          "rationale": "Deactivation changes an account's state without removing it — the record must stay addressable for audit, support, and reactivation. That rules out `DELETE` (semantic removal) and anything with side effects on `GET`. A sub-action `POST` or a `PATCH` of the status field expresses the domain transition while keeping resource-oriented URLs.",
          "optionExplanations": [
            "`DELETE` communicates that the resource is gone; clients and caches may treat `/accounts/7` as nonexistent afterward, breaking the audit and reactivation requirements.",
            "A `GET` that mutates state violates safety — crawlers, prefetchers, and monitoring probes would deactivate accounts just by touching the URL.",
            "`PUT` means 'replace the resource at this URL' — `/deactivate` names no resource, and burying the id in the body destroys addressability and idempotent-replacement semantics.",
            "Correct — model the state transition on the resource; both the sub-action `POST` and the status `PATCH` are defensible, and you should be able to argue either."
          ],
          "interviewTip": "When an action must leave the resource queryable afterward, that single requirement eliminates `DELETE` for you — lead with it. 'Soft-delete vs hard-delete' is the natural follow-up, so have a one-liner ready on status flags vs row removal."
        },
        {
          "id": "d3-d",
          "question": "An API gateway automatically re-sends any request that times out. Which endpoint below can it safely re-send without server-side deduplication, and what property makes that true?",
          "options": [
            "`PUT /shipments/3f9a` with the full shipment body — `PUT` is idempotent: replaying a complete replacement at a client-chosen URL converges to the same final state",
            "`POST /shipments` — creation endpoints are always replay-safe because the server assigns the ids",
            "`PATCH /shipments/3f9a` with `{ \"op\": \"increment\", \"retries\": 1 }` — `PATCH` is idempotent by definition",
            "None — no HTTP method may ever be retried, so the gateway feature must be disabled"
          ],
          "correctAnswerIndex": 0,
          "rationale": "Idempotency means N identical requests leave the server in the same state as one. A `PUT` of the full representation to a client-chosen URL has exactly that property — replaying it just rewrites the same bytes. A bare `POST` create mints a new resource per replay, and an increment-style `PATCH` compounds on every retry: both need deduplication (idempotency keys) before blind retries are safe.",
          "optionExplanations": [
            "Correct — full-replacement `PUT` at a known URL is the canonical retry-safe write; this is precisely why client-generated ids plus `PUT` are favored in retry-heavy systems.",
            "Server-assigned ids are exactly the problem: each replayed `POST` looks like a fresh create, so the gateway would mint duplicate shipments.",
            "`PATCH` carries no idempotency guarantee — it depends entirely on the patch semantics, and an increment operation is the textbook non-idempotent counterexample.",
            "Idempotent methods exist specifically so infrastructure can retry them; forbidding all retries throws away the guarantee the spec provides."
          ],
          "interviewTip": "When asked about retry-safe API design, volunteer the design move — 'let the client generate the id and use `PUT`' — rather than just reciting the method table. It shows you can build idempotency, not just define it."
        }
      ]
    },
    {
      "heading": "Middleware and the request pipeline",
      "paragraphs": [
        "In Express-style frameworks, a request flows through a chain of middleware functions: `(req, res, next)`. Each can inspect or modify the request, end the response, or call `next()` to pass control down the chain. Order of registration is order of execution.",
        "This is where cross-cutting concerns live: body parsing, logging, rate limiting, authentication, authorization. An auth middleware that finds no valid token responds 401 and simply never calls next() — short-circuiting the pipeline.",
        "Error handling gets its own shape: a four-argument middleware `(err, req, res, next)` registered last. Async route errors must reach it — in Express 4 that means catching and calling `next(err)` (Express 5 forwards rejected promises automatically)."
      ],
      "variants": [
        {
          "id": "d4",
          "question": "A developer registers routes first and the authentication middleware after them: `app.use('/api', routes); app.use(authMiddleware);`. What's the consequence?",
          "options": [
            "Nothing — middleware order doesn't matter in Express",
            "Express detects the mistake and reorders them at startup",
            "The routes run before auth ever executes — every /api endpoint is effectively unauthenticated",
            "The auth middleware runs twice per request"
          ],
          "correctAnswerIndex": 2,
          "rationale": "Middleware executes in registration order. A request matching /api hits the route handler, which sends a response — the chain never reaches authMiddleware. Order-dependent security is why auth registration belongs above the routes it protects.",
          "optionExplanations": [
            "Order is the single most important property of a middleware pipeline.",
            "Express has no dependency analysis; registration order is the contract, and it trusts you.",
            "Correct — and it fails open: everything works in testing, except nothing is protected.",
            "It runs zero times for matched routes, not twice — the response ends the chain first."
          ],
          "interviewTip": "Security bugs that 'fail open' (system works, protection silently absent) make strong interview stories. This ordering bug is a perfect concrete example to have in your pocket."
        },
        {
          "id": "d4-alt",
          "question": "An async route handler in Express 4 throws inside a rejected `await`, but the developer forgot to `catch` it. The four-argument error middleware is registered correctly at the end. Why does the client still hang instead of getting a clean 500?",
          "options": [
            "Registering the error handler last is the bug; it must be registered first",
            "Throwing inside `await` crashes the process instantly, so no response is possible",
            "The error middleware needs five arguments to catch async errors",
            "Express 4 does not automatically forward a rejected promise to the error handler — the rejection escapes the middleware chain, so `next(err)` is never called and the response is never sent"
          ],
          "correctAnswerIndex": 3,
          "rationale": "Express 4 only routes errors to the `(err, req, res, next)` handler when you synchronously throw or explicitly call `next(err)`. An unhandled promise rejection in an async handler is invisible to that mechanism — nothing calls `next(err)`, no response is written, and the socket stays open until it times out. The fix is a try/catch that calls `next(err)` (or an async wrapper); Express 5 forwards rejections automatically.",
          "optionExplanations": [
            "Error handlers are meant to be registered last, after the routes whose errors they catch — that ordering is correct here.",
            "An unhandled rejection may log a warning but does not synchronously crash the process; the request simply never gets a response.",
            "Four arguments is exactly what marks a middleware as an error handler; five would not be recognized as one.",
            "Correct — Express 4 needs `next(err)`; a bare async rejection bypasses the error pipeline and leaves the request hanging."
          ],
          "interviewTip": "'How do async errors reach Express error middleware?' → Express 4: you must `catch` and `next(err)` (or use `express-async-errors`/a wrapper); Express 5: rejected promises are forwarded for you."
        },
        {
          "id": "d4-c",
          "question": "A teammate registers routes before the parsers: `app.use('/api', apiRoutes); app.use(express.json());`. POSTs to `/api` now misbehave. What do the route handlers actually observe, and why?",
          "options": [
            "Express buffers each request until all registered middleware has run, so handlers still see parsed bodies",
            "`req.body` is `undefined` in every handler — the router matches first, so the request reaches the handlers before any middleware has parsed the body stream",
            "Express throws at startup because parsers must be registered before routers",
            "Handlers receive the raw body as a string and must call `JSON.parse` themselves"
          ],
          "correctAnswerIndex": 1,
          "rationale": "Registration order is execution order. A POST to `/api` matches the router immediately, the handler runs, and `express.json()` — sitting later in the chain — never gets a chance to read and parse the stream. The handler sees `req.body === undefined`, and the failure is quiet: no error, just missing data, often surfacing as a confusing `TypeError` deep in business logic.",
          "optionExplanations": [
            "There is no buffering-until-registration-complete phase; the chain runs top-down per request and stops progressing once a handler responds.",
            "Correct — parsers only populate `req.body` for middleware and handlers positioned after them; upstream handlers see nothing.",
            "Express performs no startup validation of middleware order — it trusts registration order completely, which is exactly why this class of bug ships.",
            "Without a body-parsing middleware having run, `req.body` isn't a raw string — it's simply `undefined`; the unread stream is still sitting on the request."
          ],
          "interviewTip": "Same root cause as the auth-after-routes bug — order is the contract — but this variant fails with missing data instead of missing security. Naming both consequences of one principle is a strong interview move."
        },
        {
          "id": "d4-d",
          "question": "Errors forwarded with `next(err)` skip your custom handler and fall through to Express's default HTML error page. The handler is registered last, after all routes: `app.use((err, req, res) => { res.status(500).json({ error: 'oops' }); });`. Why is it never invoked?",
          "options": [
            "Express identifies error middleware purely by function arity — a three-parameter function is treated as ordinary middleware regardless of parameter names, so the error path skips it entirely; declare all four: `(err, req, res, next)`",
            "`next(err)` can only reach handlers registered before the route that raised the error",
            "Custom JSON error responses require replacing Express's default handler via `app.set`",
            "The handler must call `next()` once without arguments before it becomes eligible to receive errors"
          ],
          "correctAnswerIndex": 0,
          "rationale": "Express inspects `fn.length` — the declared parameter count — to classify middleware. Exactly four parameters marks an error handler; anything else is regular middleware, which the error-dispatch path skips entirely. Parameter names are irrelevant, so `(err, req, res)` silently registers as a normal three-argument middleware and errors sail past it to the default handler.",
          "optionExplanations": [
            "Correct — the arity check is the contract; keep the fourth parameter even if unused (convention: `(err, req, res, next)`, often with `next` untouched).",
            "It's the reverse: `next(err)` skips remaining regular middleware and searches forward for error handlers, which is why they belong at the end.",
            "No `app.set` incantation is involved — any correctly-declared four-argument middleware that sends a response overrides the default page.",
            "There's no eligibility handshake; classification happens at registration time from the function signature, before any request flows."
          ],
          "interviewTip": "That Express dispatches on `fn.length` is a detail most candidates don't know — mentioning it (and its gotcha: rest parameters or dropping an unused arg silently changes arity) reads as real production experience."
        }
      ]
    },
    {
      "heading": "Failing well: errors, timeouts, retries",
      "paragraphs": [
        "Every await can reject. Unhandled rejections in a server context mean crashed requests or, worse, a crashed process. Route-level try/catch (or a wrapper) funneling into a central error handler gives you one place for logging and consistent error responses.",
        "Never trust an external call to return promptly — attach timeouts (AbortController) so a hung upstream doesn't pin your requests forever. Retry transient failures with exponential backoff, and only retry operations that are idempotent or de-duplicated.",
        "Separate expected failures (validation, not-found — clean 4xx responses) from unexpected ones (bugs, upstream outages — 5xx, logged loudly with a correlation id, message kept vague to the client)."
      ],
      "variants": [
        {
          "id": "d5",
          "question": "A payment provider call occasionally hangs for minutes. The team adds an aggressive automatic retry (3 attempts, no backoff, no idempotency key). What's the biggest risk?",
          "options": [
            "Duplicate charges: a 'hung' request may have actually succeeded server-side, and non-idempotent retries repeat it — while synchronized retry bursts hammer a struggling upstream",
            "The TypeScript compiler will flag the retry loop as unsafe",
            "Retrying is impossible with async/await",
            "The retries consume too much memory on the client"
          ],
          "correctAnswerIndex": 0,
          "rationale": "A timeout tells you the response didn't arrive — not that the operation didn't happen. Retrying a possibly-succeeded charge without an idempotency key risks charging twice. Backoff with jitter matters too: immediate uniform retries pile onto an already-degraded service.",
          "optionExplanations": [
            "Correct — retry safety = idempotency + backoff. Both are missing here, and money makes the failure expensive.",
            "Type checkers know nothing of retry semantics; this bug type-checks perfectly.",
            "Retry loops with async/await are routine — a for-loop with try/catch and a delay.",
            "Three retries hold trivial memory; the risk is on the money, not the RAM."
          ],
          "interviewTip": "The distilled principle for system-design rounds: 'timeouts bound your waiting, retries need idempotency, backoff protects the upstream.' Three clauses, huge signal."
        },
        {
          "id": "d5-alt",
          "question": "A service fronts a flaky third-party API. Under load, a spike of failures triggers thousands of simultaneous retries that all fire again at the exact same interval, repeatedly knocking the recovering upstream back down. Backoff is already in place. What's missing?",
          "options": [
            "Nothing — synchronized retries are the correct behavior; the upstream should just scale",
            "Jitter: adding randomness to each backoff delay so retries spread out instead of aligning into synchronized 'thundering herd' waves — and a circuit breaker to stop retrying a known-down upstream entirely",
            "The retry count is too low; raising it to 10 attempts will push through the outage",
            "Switching from exponential to linear backoff will fix the alignment"
          ],
          "correctAnswerIndex": 1,
          "rationale": "Fixed exponential backoff makes every failed caller wait the same computed delay, so they all retry in lockstep — a self-inflicted thundering herd that prevents recovery. Randomized jitter decorrelates the retries across time, and a circuit breaker trips after sustained failure to stop hammering the upstream and fail fast until it's healthy again.",
          "optionExplanations": [
            "Synchronized retry storms actively obstruct recovery; expecting the upstream to absorb a self-inflicted herd is not a real fix.",
            "Correct — jitter decorrelates the retry timing and a circuit breaker halts retries against a downed dependency.",
            "More attempts multiplies the load on an already-struggling service, deepening the outage rather than resolving it.",
            "Linear backoff still fires all callers at identical (just differently spaced) instants; without randomness they stay synchronized."
          ],
          "interviewTip": "The phrase interviewers listen for is 'exponential backoff *with jitter*' — plus a circuit breaker for sustained failure. Backoff alone still lets retries synchronize into a thundering herd."
        },
        {
          "id": "d5-c",
          "question": "Your checkout service calls a shipping-rates API with idempotent retries and jittered exponential backoff — yet during an upstream incident, in-flight requests pile up for minutes and the Node process exhausts its sockets. Which safeguard is missing?",
          "options": [
            "A higher retry cap, so requests eventually punch through the incident",
            "`Promise.race` against a `setInterval` that polls the upstream until it responds",
            "A per-attempt timeout (`AbortController` + `signal`) — retries and backoff only govern what happens *after* a failure; nothing yet bounds how long a single hung attempt may wait, so hung sockets accumulate indefinitely",
            "Moving the calls into a worker thread so hung requests only block the worker"
          ],
          "correctAnswerIndex": 2,
          "rationale": "Retry logic activates on failure — but a hung socket isn't a failure yet; it's an attempt that never resolves. Without a timeout, 'slow' never converts into 'failed', so backoff and retries sit idle while open connections accumulate and starve the process. An `AbortController` deadline per attempt turns hangs into prompt errors that the (already correct) retry machinery can handle.",
          "optionExplanations": [
            "More attempts multiplies pressure on a struggling upstream and does nothing for the attempts currently hung — the first try still never returns.",
            "Racing a promise abandons your handler's wait but doesn't cancel the underlying request — the socket stays open, which is precisely the resource being exhausted.",
            "Correct — timeouts bound the waiting; they're the trigger that lets retry-with-backoff engage at all. Without them the rest of the resilience stack is unreachable.",
            "I/O waits don't occupy the JS thread anyway; the exhausted resource is sockets/file descriptors, which worker threads share with the main process."
          ],
          "interviewTip": "A subtle point worth stating: `Promise.race`-based 'timeouts' orphan the request rather than cancel it. `AbortController` actually tears down the connection — that distinction separates senior answers from rehearsed ones."
        },
        {
          "id": "d5-d",
          "question": "To make payment retries safe, a developer generates a fresh UUID idempotency key inside the retry loop — a new key per attempt. Retries 'work', but duplicate charges still appear after timeouts. What's the flaw?",
          "options": [
            "UUIDs can collide under load, so two customers occasionally share a key",
            "Idempotency keys only function over connections that support HTTP/2",
            "The key should be derived from the card number so the server can match customers",
            "The key must be held constant across every retry of one logical operation — a fresh key per attempt makes each retry look like a brand-new payment, so the server's deduplication never matches and a possibly-succeeded charge is executed again"
          ],
          "correctAnswerIndex": 3,
          "rationale": "An idempotency key identifies the logical operation, not the network attempt. The server dedupes by recording the key with the outcome; a retry bearing the same key replays the recorded result instead of re-executing. Regenerating the key per attempt severs that link — after a timeout (where the charge may have succeeded server-side), the retry arrives with an unknown key and is processed as a second, distinct payment.",
          "optionExplanations": [
            "UUIDv4 collision probability is negligible; the duplicates here are caused deterministically by the key rotation, not by chance.",
            "Idempotency keys are just a request header plus server-side bookkeeping — the transport protocol version is irrelevant.",
            "Deriving keys from card data conflates the customer with the operation (and smuggles sensitive data into headers); one customer legitimately makes many payments.",
            "Correct — generate the key once per logical operation, outside the retry loop, and reuse it on every attempt so the server can recognize the repeat."
          ],
          "interviewTip": "The one-liner that shows mastery: 'the idempotency key's scope is the operation, not the attempt.' Follow with where it's generated — once, before the retry loop — and you've answered the implementation follow-up preemptively."
        }
      ]
    }
  ]
}

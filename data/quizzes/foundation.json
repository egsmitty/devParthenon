{
  "id": "foundation",
  "title": "Web Foundations — The Stylobate",
  "passThreshold": 0.85,
  "sections": [
    {
      "heading": "Clients and servers: who does what",
      "paragraphs": [
        "The web is a conversation between two roles. A client (usually a browser) asks for things. A server listens on a network address, receives those requests, and answers. Neither side keeps a phone line open — every exchange is a discrete request followed by a discrete response.",
        "This matters because HTTP is stateless: the server does not inherently remember your previous request. Anything that feels like memory — being logged in, a shopping cart — is rebuilt on every request from data the client sends along (cookies, tokens) or data the server looks up (a database).",
        "When an interviewer asks 'what happens when you type a URL and press Enter', this request/response boundary is the backbone of the answer."
      ],
      "summary": "The web forgets between requests, so anything that feels like memory is rebuilt each time from what the client sends along.",
      "variants": [
        {
          "id": "f1",
          "question": "HTTP is described as a *stateless* protocol. What does that actually mean?",
          "options": [
            "Each request stands alone; the server keeps no built-in memory of a client's past requests",
            "The client may only receive data from the server and can never send any of its own back",
            "Application state is allowed to live on the client, but it may never be stored on the server",
            "The server crashes outright if it ever tries to store any data in between two requests"
          ],
          "correctAnswerIndex": 0,
          "rationale": "Statelessness means the protocol itself carries no memory between requests. Each request must contain everything needed to handle it — which is exactly why cookies, sessions, and tokens exist: they re-introduce continuity on top of a stateless protocol.",
          "optionExplanations": [
            "Correct — the protocol has no built-in memory; continuity is layered on with cookies, sessions, or tokens.",
            "Clients send data all the time — form submissions, JSON bodies, uploaded files. That has nothing to do with statelessness.",
            "Servers absolutely store state (that is what databases are). The protocol just doesn't carry it between requests automatically.",
            "Servers store data constantly (databases, caches). Statelessness is a property of the protocol, not a restriction on server storage."
          ],
          "interviewTip": "A favorite follow-up is 'so how does login work over a stateless protocol?' Be ready to explain cookies and session/token round-tripping in one breath."
        },
        {
          "id": "f1-alt",
          "question": "You load a product page, then reload it a second later while logged in, and the server still 'knows' you and your cart. Given that HTTP is stateless, how does the server actually recognize you on that second request?",
          "options": [
            "The server holds a persistent open connection to your browser and reads your identity straight off it",
            "The reload re-sends a `Cookie` header or token that the server uses to look you up again",
            "Statelessness applies only to the first request; every request after it is automatically stateful",
            "The browser stamps each request with a permanent trusted hardware ID that the server reads off"
          ],
          "correctAnswerIndex": 1,
          "rationale": "Because the protocol remembers nothing, the *client* must re-present its identity on every single request — typically a session cookie or bearer token the server uses to re-load your session and cart from storage. Continuity is rebuilt each time, not preserved by the protocol.",
          "optionExplanations": [
            "HTTP does not hold an open identity-bearing line; even keep-alive TCP connections carry independent, self-contained requests with no inherent memory of who you are.",
            "Correct — the client re-sends a cookie or token on every request, and the server re-derives your identity and cart from it.",
            "Statelessness applies to every request equally; there is no 'first request' exception that flips the protocol into a stateful mode.",
            "Browsers send no permanent trusted hardware ID; identity rides in cookies/tokens the app issues, precisely because the protocol itself carries none."
          ],
          "interviewTip": "Turn it around: 'the protocol forgets, so the client must remind it every request.' Naming the exact carrier — cookie or Authorization header — shows you know where the continuity physically lives."
        },
        {
          "id": "f1-c",
          "question": "Your team adds a second app server behind a load balancer. A teammate objects: 'users who logged in through server 1 will break the moment the balancer routes their next request to server 2.' Under stateless HTTP, what actually determines whether this works?",
          "options": [
            "It cannot work, because HTTP permanently pins each authenticated client to the server that first saw it",
            "It works on its own, because the load balancer mirrors each server's memory to the other in real time",
            "It works if every request carries its credential and session data lives in storage any server can read",
            "It works only if users are forced to log in again whenever the balancer switches which server they hit"
          ],
          "correctAnswerIndex": 2,
          "rationale": "Statelessness is exactly why horizontal scaling works: each request is self-contained, carrying its own credential, so any server can handle it — provided the session lookup hits shared storage (a database, Redis) rather than one machine's RAM. The teammate's fear is only valid if session state was trapped in server 1's memory.",
          "optionExplanations": [
            "HTTP pins nothing; there is no protocol-level binding between a client and a server. 'Sticky sessions' exist as a load-balancer workaround, not an HTTP rule.",
            "Load balancers route packets; they do not replicate application memory. Any real-time memory mirroring would be an application architecture choice, not balancer magic.",
            "Correct — self-contained requests plus shared session storage means any server can answer any request, which is the payoff of building on a stateless protocol.",
            "No re-login is needed: the same cookie or token travels with every request regardless of which server receives it, and each server can validate it independently."
          ],
          "interviewTip": "If someone proposes sticky sessions, name the trade-off: they paper over in-memory session state but break the moment a server dies. Externalizing sessions to Redis or a database is the answer interviewers want to hear."
        },
        {
          "id": "f1-d",
          "question": "A junior dev inspects a response, sees `Connection: keep-alive`, and concludes: 'HTTP isn't really stateless — the server is holding our connection open, so it must be remembering us.' What is wrong with that reasoning?",
          "options": [
            "Keep-alive reuses the TCP connection to skip handshakes; the requests over it stay independent",
            "Nothing is wrong — keep-alive was introduced precisely to turn HTTP into a stateful protocol",
            "Keep-alive does persist your session, but only over HTTPS where the TLS layer quietly stores it",
            "The header is purely informational and has no real effect on how the connection is handled at all"
          ],
          "correctAnswerIndex": 0,
          "rationale": "Statelessness lives at the level of protocol semantics, not transport plumbing. Keep-alive (and HTTP/2 multiplexing) reuses the expensive TCP/TLS setup for performance, but each request over that connection must still be fully self-describing — the server derives nothing about your identity from the socket itself.",
          "optionExplanations": [
            "Correct — connection reuse is a transport optimization; every request over the reused socket is still processed as an independent, self-contained unit.",
            "Keep-alive exists to amortize handshake cost, not to add memory. HTTP's request/response semantics are unchanged by it.",
            "TLS secures the channel; it does not store application sessions, and keep-alive behaves the same with or without HTTPS.",
            "The header genuinely governs connection reuse (especially in HTTP/1.x) — it just has no bearing on whether the server remembers you."
          ],
          "interviewTip": "Separating layers cleanly — 'TCP reuse is performance, statelessness is semantics' — is a strong systems signal. It also sets up a natural segue to HTTP/2 multiplexing if the interviewer wants to go deeper."
        }
      ]
    },
    {
      "heading": "Anatomy of an HTTP request",
      "paragraphs": [
        "An HTTP request is plain structured text: a method and path (`GET /products/42 HTTP/1.1`), then headers (key-value metadata like `Accept: application/json` or `Cookie: session=abc`), then optionally a body — the payload.",
        "The method declares intent. `GET` reads and should never change anything. `POST` creates or triggers. `PUT` replaces a resource entirely, `PATCH` updates part of it, and `DELETE` removes it.",
        "A useful lens is idempotency: `GET`, `PUT`, and `DELETE` can be safely retried — doing them twice leaves the world in the same state. `POST` is not idempotent, which is why your bank warns you not to click 'Pay' twice."
      ],
      "summary": "Each request states its intent through its method, and picking the right one decides whether a retry is safe or repeats the action.",
      "variants": [
        {
          "id": "f2",
          "question": "A payment API endpoint charges a card when called. A flaky network means the client may retry requests automatically. Which method choice best reflects HTTP semantics for 'create a new charge'?",
          "options": [
            "GET, since it is the simplest method and the easiest to test directly in a browser",
            "PUT, since a charge should be freely retryable and PUT is safe to repeat blindly",
            "POST, with the server using an idempotency key to de-duplicate any retried charge",
            "DELETE, since charging a card ultimately removes money from the customer's account"
          ],
          "correctAnswerIndex": 2,
          "rationale": "Creating a new resource is POST's job. Since POST is not idempotent, real payment APIs (like Stripe) let the client send an idempotency key so a retried POST doesn't create a second charge — the semantics stay honest and retries stay safe.",
          "optionExplanations": [
            "GET must never cause side effects. Browsers, proxies, and prefetchers freely repeat GETs — a GET that charges money could fire from a link preview.",
            "PUT means 'replace the resource at this URL with this body'. A new charge has no URL yet, and blind retry-safety without an idempotency scheme is exactly the trap.",
            "Correct — POST for creation, plus an idempotency key to make retries safe in practice.",
            "DELETE removes the resource identified by the URL. Charging money is creating a charge record, not deleting anything."
          ],
          "interviewTip": "Mentioning idempotency keys when discussing POST retries is a strong signal you've thought about real-world API failure modes."
        },
        {
          "id": "f2-alt",
          "question": "A mobile app lets a user edit their whole profile by sending the full profile object to `/users/42/profile`. If the request times out and the app silently re-sends the identical body, which method makes that automatic retry inherently safe — no duplicate side effects — without any extra de-duplication logic?",
          "options": [
            "`PATCH`, because a partial update is always safe to apply repeatedly by definition",
            "`GET`, because reads can be repeated freely and this is really just fetching data",
            "`POST`, because it is the standard verb whenever you need to send a body of data",
            "`PUT`, because replacing the resource with the same body twice leaves it unchanged"
          ],
          "correctAnswerIndex": 3,
          "rationale": "`PUT` replaces the resource at a known URL with the supplied representation. Applying the identical replacement twice yields the same final state as applying it once — that is idempotency, so a blind retry is safe without an idempotency key. The resource already has a URL (`/users/42/profile`), which is exactly what `PUT` requires.",
          "optionExplanations": [
            "`PATCH` is not guaranteed idempotent — a partial update like 'increment balance by 10' applied twice diverges; safety depends on the patch's semantics, not the method.",
            "`GET` is safe to repeat but it only reads; it cannot perform the profile edit the scenario requires, so it is the wrong tool entirely.",
            "`POST` is not idempotent; a re-sent `POST` may create a second record or re-trigger an action, so an automatic retry is unsafe by default.",
            "Correct — `PUT` is a full replacement to a known URL; repeating the same replacement leaves identical state, so the retry is inherently safe."
          ],
          "interviewTip": "Don't lump `PATCH` in with `PUT` as 'both safe to retry' — `PATCH` is idempotent only if the patch itself is. That distinction is a common trap."
        },
        {
          "id": "f2-c",
          "question": "A client sends `DELETE /orders/99` and receives `204 No Content`. A network-layer retry re-sends the identical request and gets `404 Not Found`. A teammate declares: 'see — different responses, so `DELETE` isn't actually idempotent.' Are they right?",
          "options": [
            "Yes — an idempotent method must, by definition, return the very same status code on every repetition",
            "No — idempotency is about resulting server state, not identical responses; the order is gone either way",
            "Yes — the `404` on retry proves the second request corrupted the server state the first had left intact",
            "No — but only because the server silently suppressed the second delete; a real one would break idempotency"
          ],
          "correctAnswerIndex": 1,
          "rationale": "Idempotency means N identical requests leave the server in the same state as one request. The order is deleted after the first call and still deleted after the retry — state is unchanged. The differing status codes are just the server truthfully describing what it found each time; nothing in the spec requires repeat responses to match.",
          "optionExplanations": [
            "The spec defines idempotency in terms of the effect on the server, not the response. `DELETE` then `404` on retry is the textbook-compliant behavior.",
            "Correct — same final state ('order 99 does not exist') after one call or ten; the response merely reports what the server found at that moment.",
            "Nothing was corrupted: the `404` reflects that the resource was already gone, which is exactly the state the first `DELETE` produced.",
            "The server executed both requests honestly; the second simply found nothing to delete. No suppression mechanism is needed for `DELETE` to be idempotent."
          ],
          "interviewTip": "Define idempotency precisely — 'same server state after N calls as after 1' — before answering retry questions. Interviewers plant the differing-response red herring specifically to see if your definition is about state or about responses."
        },
        {
          "id": "f2-d",
          "question": "A team ships `GET /reports/latest/regenerate`, which rebuilds an expensive report and emails it to stakeholders whenever it is hit. Weeks later, stakeholders complain about duplicate report emails arriving at odd hours when nobody was using the app. What is the root design flaw?",
          "options": [
            "The URL is too long, which causes some intermediary proxies to replay the request on their own",
            "Regeneration replaces the old report file, so the endpoint should have been `PUT` to stay idempotent",
            "`GET` responses get cached by intermediaries, and the duplicate emails are being sent from those caches",
            "`GET` must be safe, but prefetchers, crawlers, and monitors fire GETs on their own; it should be `POST`"
          ],
          "correctAnswerIndex": 3,
          "rationale": "The entire web infrastructure — browser prefetching, link previews in chat apps, search crawlers, uptime monitors — assumes GETs are safe to issue speculatively and repeatedly. Hang a side effect (regenerating, emailing) off a GET and that machinery triggers it invisibly. State-changing actions belong on `POST` (or another non-safe method) precisely so automated agents leave them alone.",
          "optionExplanations": [
            "URL length has nothing to do with replays; well-behaved intermediaries repeat GETs because GETs are declared safe, regardless of URL size.",
            "`PUT` would make the *state change* retry-safe, but the email dispatch is a side effect no method makes idempotent — and the real bug is that automated agents trigger the endpoint at all, which switching to `PUT` alone doesn't fully stop since the deeper contract violated is safety.",
            "Caches store and serve responses; they never execute application logic like sending email. A cache would actually *suppress* origin hits, not multiply emails.",
            "Correct — the endpoint violates GET's safety contract, so prefetchers, crawlers, and monitors trigger the regeneration and emails autonomously; a `POST` keeps automated GET traffic from causing side effects."
          ],
          "interviewTip": "Distinguish *safe* (no side effects: `GET`, `HEAD`) from *idempotent* (repeat leaves same state: `GET`, `PUT`, `DELETE`). All safe methods are idempotent, but not vice versa — interviewers love asking for that exact Venn diagram."
        }
      ]
    },
    {
      "heading": "Response status codes: the server's verdict",
      "paragraphs": [
        "Status codes are grouped by their first digit. 2xx: success (`200 OK`, `201 Created`, `204 No Content`). 3xx: redirection (`301` moved permanently, `302` moved temporarily, `304` not modified — use your cache). 4xx: the client did something wrong. 5xx: the server failed.",
        "The 4xx family is the one interviewers probe. `400` malformed request, `401` not authenticated (who are you?), `403` authenticated but not allowed (I know who you are — no), `404` resource doesn't exist, `409` conflict, `429` too many requests.",
        "The 401 vs 403 distinction trips people up constantly: 401 means 'log in first'; 403 means 'logging in won't help'."
      ],
      "summary": "The status code is the server's short verdict, and the number's first digit alone tells you who to blame and what to do next.",
      "variants": [
        {
          "id": "f3",
          "question": "A logged-in user with a valid session tries to open the admin dashboard, but they are not an admin. Which status code should the server return?",
          "options": [
            "403 Forbidden",
            "404 Not Found",
            "500 Internal Server Error",
            "401 Unauthorized"
          ],
          "correctAnswerIndex": 0,
          "rationale": "The user is authenticated (we know who they are) but not authorized (their role doesn't permit this). That is precisely 403 Forbidden. Despite its confusing name, 401 'Unauthorized' actually means unauthenticated.",
          "optionExplanations": [
            "Correct — authenticated but lacking permission is 403.",
            "404 is defensible only as a deliberate strategy to hide the resource's existence (GitHub does this for private repos) — but as the straightforward semantic answer, it's wrong.",
            "Nothing failed on the server. 5xx codes mean the server itself errored, which would page an on-call engineer for what is actually normal access control.",
            "401 means the request lacks valid credentials — 'log in and try again'. This user is already logged in; logging in again changes nothing."
          ],
          "interviewTip": "Say the mnemonic out loud in interviews: '401 = who are you? 403 = I know who you are, and no.' Bonus points for noting some APIs return 404 to avoid leaking resource existence."
        },
        {
          "id": "f3-alt",
          "question": "An API client calls a protected endpoint but its access token has fully expired, so the server can no longer establish who the caller is. Following HTTP semantics, which status code most precisely signals 'your credentials are missing or invalid — re-authenticate'?",
          "options": [
            "400 Bad Request",
            "401 Unauthorized",
            "429 Too Many Requests",
            "403 Forbidden"
          ],
          "correctAnswerIndex": 1,
          "rationale": "An expired token means the request effectively lacks valid credentials, so the server cannot identify the caller. That is the exact meaning of 401 — despite the word 'Unauthorized', it signals *unauthenticated*: re-present valid credentials and try again.",
          "optionExplanations": [
            "400 is for a syntactically malformed or unparseable request. An expired token is a well-formed request with an authentication problem, not a parsing problem.",
            "Correct — missing or invalid credentials the server can't authenticate is 401; the client should refresh or re-obtain a token.",
            "429 signals rate limiting — too many requests in a window. Token expiry has nothing to do with request volume.",
            "403 means the identity is known but not permitted; here the identity can't even be established, so re-authenticating is the fix, which is 401's message, not 403's."
          ],
          "interviewTip": "Flip the primary question's trap: here the credentials are the problem, so it's 401, not 403. Note that 401 responses should include a `WWW-Authenticate` header telling the client how to authenticate."
        },
        {
          "id": "f3-c",
          "question": "While authenticated, you request another tenant's private document by guessing its ID, and the API returns `404 Not Found` — even though you know the document exists. Why would a well-designed API choose this response deliberately?",
          "options": [
            "The server hit an internal routing bug that mapped the request onto a handler that does not exist",
            "The API is simply wrong here — HTTP mandates a `403` whenever a resource exists but access is denied",
            "A `403` would confirm the resource exists; one uniform `404` stops attackers enumerating IDs at all",
            "`404` responses are meaningfully cheaper for the server to generate than `403` responses are to build"
          ],
          "correctAnswerIndex": 2,
          "rationale": "A `403` on a guessed ID leaks information: 'something is here, you just can't see it.' By collapsing 'doesn't exist' and 'exists but private' into one indistinguishable `404`, the API denies attackers an oracle for mapping out private data. The HTTP spec explicitly permits `404` in place of `403` for exactly this reason — GitHub does it for private repositories.",
          "optionExplanations": [
            "Nothing malfunctioned — the routing worked and access control ran; the `404` is a deliberate policy choice, not an error in dispatch.",
            "The spec allows a server that wishes to hide a resource's existence to respond `404` instead of `403`; the mandate this option imagines does not exist.",
            "Correct — uniform `404`s make existing-but-forbidden indistinguishable from nonexistent, closing off resource-enumeration attacks.",
            "Both are trivial responses to produce; the choice is driven by information leakage, not compute cost."
          ],
          "interviewTip": "Volunteering the security angle — '403 confirms existence, so sensitive APIs return 404 instead' — turns a status-code question into a security answer. Cite GitHub's private-repo behavior as your concrete example."
        },
        {
          "id": "f3-d",
          "question": "A logged-in user tries to delete a teammate's comment and lacks permission. The server responds `401`, and the SPA — following standard convention — clears the session and redirects to the login page, trapping the user in a re-login loop. Which single change fixes both the semantics and the loop?",
          "options": [
            "Return `403 Forbidden` — the user is authenticated but not permitted, so the client shows a denial note",
            "Return `200` with an error flag in the body so the client stops reacting to HTTP status codes altogether",
            "Return `500`, since the delete the user attempted ultimately did not succeed on the server at all",
            "Keep the `401` but have the login page auto-submit the saved credentials so the loop turns invisible"
          ],
          "correctAnswerIndex": 0,
          "rationale": "Clients automate against status codes as a contract: `401` conventionally means 'credentials invalid — re-authenticate,' which is why the SPA nukes the session. The user's credentials are fine; their *role* is insufficient. That is `403`, which client code handles by showing a denial message rather than restarting the auth flow.",
          "optionExplanations": [
            "Correct — `403` states 'identity confirmed, permission denied,' so the client's re-auth machinery stays out of it and the loop disappears.",
            "Tunneling errors through `200` breaks caches, monitoring, and every intermediary that reads status codes; it treats the symptom by discarding the contract.",
            "Nothing failed on the server — access control worked exactly as designed. A `5xx` would page on-call for correct behavior.",
            "Auto-submitting login hides the loop while re-authenticating a user whose authentication was never the problem; the wrong code still drives the wrong client behavior."
          ],
          "interviewTip": "Frame status codes as a machine-readable contract, not decoration: interceptors key `401` to token-refresh/login flows and `403` to denial UI. Explaining the *client-side consequence* of a wrong code is what separates senior answers."
        }
      ]
    },
    {
      "heading": "DNS: from name to address",
      "paragraphs": [
        "Computers route packets to IP addresses, not names. DNS translates `example.com` into `93.184.216.34`. Your machine first checks local caches, then asks a resolver, which walks the hierarchy: root servers point to the `.com` TLD servers, which point to example.com's authoritative nameserver, which returns the record.",
        "Records have types: `A` maps a name to an IPv4 address, `AAAA` to IPv6, `CNAME` aliases one name to another, `MX` routes mail, `TXT` holds arbitrary text (often domain-ownership proofs).",
        "Every record carries a TTL — how long resolvers may cache it. Low TTLs mean fast propagation of changes but more lookup traffic; high TTLs the reverse. 'DNS propagation delay' is mostly just caches waiting for TTLs to expire."
      ],
      "summary": "DNS turns a name into an address, and the delay you feel after a change is just old cached answers waiting for their TTL to expire.",
      "variants": [
        {
          "id": "f4",
          "question": "You change your site's A record to point at a new server, but some visitors still reach the old server for hours. Why?",
          "options": [
            "The new A record only applies to fresh visitors who have never once loaded the site before",
            "The old server is actively intercepting the traffic that was meant to reach the new one",
            "Resolvers and operating systems cache the DNS answer until the record's TTL finally expires",
            "DNS changes like this one require the visitor's browser to be updated before they take effect"
          ],
          "correctAnswerIndex": 2,
          "rationale": "DNS answers are cached at many layers — the OS, the router, the ISP's resolver — each honoring the TTL that was on the record when it was fetched. Until those caches expire, they keep serving the old IP. This is why you lower the TTL *before* a planned migration.",
          "optionExplanations": [
            "DNS has no concept of new vs returning visitors; it's purely name-to-address lookup with caching.",
            "The old server does nothing active — traffic reaches it because cached answers still name its IP.",
            "Correct — layered caching plus TTL is the entire mechanism behind 'propagation delay'.",
            "Browsers have small DNS caches but need no update; the delay lives in resolver caches, not software versions."
          ],
          "interviewTip": "The pro move in a systems interview: 'before migrating, drop the TTL to 60 seconds a day in advance, migrate, then raise it back.'"
        },
        {
          "id": "f4-alt",
          "question": "You want to point `blog.example.com` at a third-party host that manages its own rotating set of IP addresses, so you never have to update an IP yourself when they change servers. Which DNS record type is designed for this 'alias one name to another name' job?",
          "options": [
            "An `MX` record that points `blog.example.com` at the third-party host",
            "A `TXT` record that simply contains the third-party host's domain name",
            "An `A` record that lists all of the host's current IP addresses by hand",
            "A `CNAME` record aliasing `blog.example.com` to the host's own domain"
          ],
          "correctAnswerIndex": 3,
          "rationale": "A `CNAME` aliases one name to another name, so `blog.example.com` resolves by following through to the provider's hostname — and whatever IPs *they* map that name to are used automatically. You never touch an IP, which is exactly the delegation you want.",
          "optionExplanations": [
            "`MX` records route email to mail servers; they have nothing to do with resolving a web hostname to an address.",
            "`TXT` holds arbitrary text (like verification strings) and is never followed as an address alias by resolvers doing name resolution.",
            "An `A` record hard-codes specific IPs, so you'd have to edit it every time the provider rotates servers — the opposite of hands-off.",
            "Correct — `CNAME` aliases your name to the provider's name, so their IP changes flow through without you editing anything."
          ],
          "interviewTip": "Know the classic `CNAME` gotcha: you can't put a `CNAME` on a zone apex (`example.com` itself) alongside other required records — which is why providers offer `ALIAS`/`ANAME` or flattening for the root domain."
        },
        {
          "id": "f4-c",
          "question": "The `A` record for `api.example.com` carries a TTL of 86400 (24 hours), and you must cut over to a new IP next Friday with minimal disruption. What is the correct TTL play?",
          "options": [
            "On Friday, change the IP and set the TTL to 30 in one edit, so the low TTL takes effect for everyone at once",
            "Lower the TTL well before Friday, let old answers age out of caches, switch the IP, then raise the TTL back",
            "Set the TTL to 0 permanently, so that resolver caches can never become a factor in any future change again",
            "Skip the TTL entirely — it governs only `CNAME` chains, so an `A` change propagates on the resolvers' schedule"
          ],
          "correctAnswerIndex": 1,
          "rationale": "The TTL travels *with* the cached answer: resolvers holding the old record honor the 86400 that was attached when they fetched it, so a TTL you set at cutover time reaches no one who already has the old answer. You must lower the TTL at least one full old-TTL period in advance, so that by Friday every cache holds a short-lived answer that expires quickly after the switch.",
          "optionExplanations": [
            "Caches that fetched the record before Friday obey the old 86400 they stored — your new TTL is only seen by resolvers fetching *after* the change, which is exactly the population that didn't need it.",
            "Correct — pre-lowering by at least the old TTL guarantees all caches hold short-lived answers at cutover; raising it afterward restores cheap, low-traffic resolution.",
            "A TTL of 0 (or near-zero) forever means every request pays a fresh lookup — needless latency and resolver load — and some resolvers clamp or ignore extreme values anyway.",
            "TTL applies to every cached record type, `A` included; there is no `CNAME`-only rule, and ignoring it is how you get a day of split traffic."
          ],
          "interviewTip": "State the timing rule crisply: 'the TTL you need at cutover must be set at least one old-TTL period beforehand.' Interviewers use this to test whether you understand that caching decisions were made in the past, not at the moment you edit the zone."
        },
        {
          "id": "f4-d",
          "question": "Users on an IPv6-only mobile carrier (no IPv4 path, no NAT64) report your site never loads, while everyone else is fine. Your zone currently contains just one `A` record for the hostname. Which record type must you add so their devices can resolve an address they can actually reach?",
          "options": [
            "A second `A` record carrying a different IPv4 address, added for redundancy",
            "A `TXT` record that publicly announces the site now supports IPv6 clients",
            "An `MX` record that points the hostname at an IPv6-capable mail gateway",
            "An `AAAA` record mapping the hostname to your server's own IPv6 address"
          ],
          "correctAnswerIndex": 3,
          "rationale": "An `A` record answers only IPv4 queries; an IPv6-only client asks for `AAAA` and gets nothing usable back. Publishing an `AAAA` record alongside the `A` record (dual-stack) gives those clients a reachable IPv6 address while IPv4 clients continue using the `A` record.",
          "optionExplanations": [
            "More IPv4 addresses cannot help a client with no IPv4 path at all — redundancy within an unreachable family is still unreachable.",
            "`TXT` records are inert text for humans and verification tooling; resolvers never use them to route traffic anywhere.",
            "`MX` records direct *email* delivery for the domain; they play no role in resolving a website's hostname for browsers.",
            "Correct — `AAAA` is the IPv6 counterpart of `A`; adding it lets IPv6-only clients resolve and connect, completing a dual-stack setup."
          ],
          "interviewTip": "Pair the record knowledge with the client behavior: dual-stacked browsers race both families via Happy Eyeballs and prefer whichever connects first. Mentioning that turns a flashcard answer into a networking answer."
        }
      ]
    },
    {
      "heading": "The DOM: the page as a living tree",
      "paragraphs": [
        "When the browser parses HTML, it builds the DOM — a tree of node objects in memory. The HTML file is the blueprint; the DOM is the standing building. JavaScript never edits your HTML file; it mutates this tree, and the browser repaints to match.",
        "Every DOM mutation can trigger style recalculation, layout (geometry), and paint. Touching the DOM is orders of magnitude more expensive than touching JavaScript objects — which is the entire reason virtual-DOM libraries like React exist: batch the changes, compute the minimal diff, touch the real DOM as little as possible.",
        "The DOM is also live: `document.querySelector` hands you a reference into the tree itself, not a copy. Change it, and the page changes."
      ],
      "summary": "The DOM is a live tree the browser draws from, and touching it is slow, so change it as little as possible and batch your updates.",
      "variants": [
        {
          "id": "f5",
          "question": "Why is repeatedly updating the real DOM inside a loop (e.g. appending 1,000 rows one at a time) considered a performance mistake?",
          "options": [
            "Each mutation can force the browser to recalc styles, layout, and paint, so unbatched work grows",
            "JavaScript loops are inherently slow no matter what the body of the loop actually happens to be doing",
            "The DOM turns read-only for the duration of a loop and silently drops every write you attempt",
            "The DOM can only hold a fixed maximum number of elements before it starts to error out on you"
          ],
          "correctAnswerIndex": 0,
          "rationale": "The cost isn't the loop — it's that every insertion can invalidate layout and trigger reflow/repaint. Batching (build a DocumentFragment, append once) or diffing (React) collapses 1,000 potential reflows into one.",
          "optionExplanations": [
            "Correct — reflow and repaint are the expensive steps, and unbatched mutations repeat them needlessly.",
            "Plain JS loops execute millions of iterations per second; the loop is innocent, the per-iteration DOM work is guilty.",
            "The DOM is fully writable at any time; nothing is dropped — that's why all 1,000 slow reflows actually happen.",
            "There is no meaningful hard cap; huge DOMs get slow, but they don't error at a limit."
          ],
          "interviewTip": "If asked 'why does React exist', anchor your answer here: real-DOM mutations are expensive, so React batches state changes and commits a minimal diff."
        },
        {
          "id": "f5-alt",
          "question": "In a loop over a list, your code writes `el.style.width` to grow an element and then immediately reads `el.offsetHeight` on each iteration, and the page becomes janky. What specific pattern is making this so slow?",
          "options": [
            "Reading `offsetHeight` allocates a large fresh copy of the whole DOM each time that it runs",
            "Interleaving a write with a read of a layout property forces a synchronous reflow every pass",
            "`style.width` is actually an invalid property, so the browser keeps retrying the assignment",
            "Reading and writing the DOM in one function is disallowed and gets silently throttled for you"
          ],
          "correctAnswerIndex": 1,
          "rationale": "The browser batches style writes and flushes layout lazily — but reading a geometry property like `offsetHeight` forces it to compute layout *now* to give an accurate answer. Doing that read right after a write, every iteration, defeats batching and triggers a synchronous reflow each time. This is layout thrashing; the fix is to batch all reads, then all writes.",
          "optionExplanations": [
            "Reading `offsetHeight` copies nothing; it returns a number. The cost is the forced layout recalculation it triggers, not any allocation.",
            "Correct — write-then-read of a layout property forces a synchronous reflow per iteration, the definition of layout thrashing.",
            "`style.width` is perfectly valid; there is no invalid-property retry loop involved.",
            "Reading and writing the DOM together is fully allowed; nothing is throttled or blocked — it just forces expensive synchronous layouts."
          ],
          "interviewTip": "Name the fix crisply: 'batch reads, then batch writes' — or reach for `requestAnimationFrame` / `getBoundingClientRect` measured up front. Saying 'layout thrashing' by name signals real DOM-performance depth."
        },
        {
          "id": "f5-c",
          "question": "To render 500 search results, a colleague builds each `<li>` inside a detached `DocumentFragment` and appends the fragment to the live `<ul>` once at the end. Why is this dramatically cheaper than appending each `<li>` to the live list as it's built?",
          "options": [
            "`DocumentFragment` compresses its child nodes so they take up far less memory once inserted",
            "Fragments are rendered on a background thread, freeing the main thread during construction",
            "A detached fragment isn't in the rendered tree, so its inserts skip layout till one live append",
            "The browser skips the HTML parsing step entirely once the nodes arrive wrapped up in a fragment"
          ],
          "correctAnswerIndex": 2,
          "rationale": "Reflow and repaint are only in play for nodes attached to the rendered document. A detached `DocumentFragment` is off-stage: you can hammer it with hundreds of mutations for free. When the fragment is appended, its children move into the live tree in a single operation, collapsing what could have been hundreds of layout invalidations into one.",
          "optionExplanations": [
            "Fragments store perfectly ordinary nodes with no compression; the win is *when* rendering work happens, not how much memory nodes take.",
            "All of this DOM construction runs on the main thread; fragments change the batching of rendering work, not which thread executes it.",
            "Correct — off-tree mutations are invisible to the renderer, so the expensive style/layout/paint pipeline runs once at the single live insertion.",
            "These nodes are built via the DOM API, not parsed from markup — and parsing cost isn't the bottleneck; repeated live-tree layout work is."
          ],
          "interviewTip": "Frame `DocumentFragment` as manual batching — 'the same principle React's reconciler automates.' Connecting a vanilla-DOM technique to the framework's reason for existing is a high-signal move."
        },
        {
          "id": "f5-d",
          "question": "This loop never terminates: `const divs = document.getElementsByTagName('div'); for (let i = 0; i < divs.length; i++) { document.body.appendChild(document.createElement('div')); }` — what property of the DOM explains the hang?",
          "options": [
            "`getElementsByTagName` returns a *live* collection, so each new `<div>` grows `divs.length` further",
            "`appendChild` is asynchronous, so the loop body outruns the insertions and the two of them deadlock",
            "Creating elements inside a loop leaks memory until the tab finally dies, which just resembles a hang",
            "`for` loops cannot read `length` off a DOM collection, so the condition always evaluates to `true`"
          ],
          "correctAnswerIndex": 0,
          "rationale": "The DOM is live, and so are the classic collection APIs: `getElementsByTagName` hands back an `HTMLCollection` that reflects the tree *right now*. Every iteration appends a `<div>`, the collection instantly reports one more element, and `i < divs.length` can never become false. A static snapshot — `document.querySelectorAll('div')` or a copied array — breaks the feedback loop.",
          "optionExplanations": [
            "Correct — the live `HTMLCollection` updates as the tree changes, so the loop keeps moving its own goalposts; snapshot it first and the loop terminates.",
            "`appendChild` is fully synchronous — the node is in the tree before the next statement runs. There is no async race here, just a self-growing bound.",
            "Memory grows because the loop is infinite, not the other way around; the causal bug is the live collection feeding the loop condition.",
            "Reading `.length` from an `HTMLCollection` works fine anywhere — that's exactly how the loop keeps observing the ever-growing count."
          ],
          "interviewTip": "Know which APIs are live and which are static: `getElementsByTagName`/`getElementsByClassName` and `node.children` are live; `querySelectorAll` returns a static `NodeList`. That one-liner defuses a whole family of trick questions."
        }
      ]
    },
    {
      "heading": "Putting it together: the life of a page load",
      "paragraphs": [
        "Full circle: you press Enter. DNS resolves the name to an IP. The browser opens a TCP connection (plus a TLS handshake for HTTPS) and sends an HTTP GET. The server responds with a status code, headers, and an HTML payload.",
        "The browser parses HTML into the DOM, discovers linked CSS and JS, fetches those (more HTTP requests), builds render information, and paints. JavaScript then hooks into the DOM to make the page interactive.",
        "Every pillar above this foundation is a deepening of one step: React and CSS live in the render/DOM phase, Next.js decides where HTML gets generated, Node is the server answering, databases are where the server gets its facts."
      ],
      "summary": "Loading a page is one clear chain — resolve, connect, request, parse, paint — and everything you'll learn later just deepens one of those steps.",
      "variants": [
        {
          "id": "f6",
          "question": "Put these page-load steps in the order they occur: (A) browser paints pixels, (B) DNS lookup, (C) HTML parsed into DOM, (D) HTTP request sent.",
          "options": [
            "B → C → D → A",
            "C → B → D → A",
            "B → D → C → A",
            "D → B → A → C"
          ],
          "correctAnswerIndex": 2,
          "rationale": "You can't send a request without an IP (DNS first), can't parse a response you haven't requested (request before parse), and can't paint a tree you haven't built (parse before paint). B → D → C → A.",
          "optionExplanations": [
            "There is no HTML to parse before the HTTP response arrives — the request must come before parsing.",
            "Parsing HTML first is impossible; the HTML only exists client-side after DNS + request + response.",
            "Correct — resolve the name, send the request, parse the response into the DOM, paint.",
            "The request can't be addressed before DNS gives you an IP, and painting can't precede having a DOM to paint."
          ],
          "interviewTip": "'Walk me through what happens when you hit Enter on a URL' is a top-5 interview question. Practice narrating this chain in under 90 seconds, then offer to zoom into any step."
        },
        {
          "id": "f6-alt",
          "question": "For an HTTPS page load, order these four events as they actually occur: (W) TLS handshake completes, (X) browser establishes the TCP connection, (Y) server sends back the HTML response, (Z) browser sends the HTTP GET request.",
          "options": [
            "W → X → Z → Y",
            "X → Z → W → Y",
            "Z → X → W → Y",
            "X → W → Z → Y"
          ],
          "correctAnswerIndex": 3,
          "rationale": "TCP is established first (X), then the TLS handshake runs on top of that connection to secure it (W), and only over the now-encrypted channel does the browser send the HTTP GET (Z), after which the server returns the HTML (Y). You can't encrypt a connection you haven't opened, and you can't send an HTTPS request before the channel is secured.",
          "optionExplanations": [
            "TLS cannot complete before a TCP connection exists to carry the handshake; the connection must come first.",
            "This puts the request before TLS finishes, meaning the HTTP GET would travel unencrypted — which defeats HTTPS and isn't how the browser sequences it.",
            "This sends the GET before TCP is even established, which is impossible — there is no connection to send over yet.",
            "Correct — TCP connect, then TLS handshake over it, then the encrypted GET, then the HTML response."
          ],
          "interviewTip": "For HTTPS, remember the layering: TCP first, TLS on top, HTTP inside. Bonus depth: mention that TLS 1.3 cut the handshake to one round trip, and that QUIC/HTTP/3 folds transport and crypto setup together to save round trips."
        },
        {
          "id": "f6-c",
          "question": "On a cold page load, order these four events as they actually occur: (P) the render-blocking stylesheet is fetched, (Q) the resolver returns the site's IP address, (R) the first pixels are painted, (S) the HTML response begins arriving.",
          "options": [
            "Q → P → S → R",
            "Q → S → P → R",
            "S → Q → P → R",
            "Q → S → R → P"
          ],
          "correctAnswerIndex": 1,
          "rationale": "DNS must resolve before anything can be requested (Q first). The stylesheet's URL lives inside the HTML, so the browser can't discover — let alone fetch — it until the HTML starts arriving and being parsed (S before P). And because the stylesheet is render-blocking, the browser withholds first paint until that CSS is in hand (P before R). Q → S → P → R.",
          "optionExplanations": [
            "The browser learns the stylesheet exists from a `<link>` tag in the HTML; it cannot fetch a URL it hasn't seen, so P cannot precede S.",
            "Correct — resolve the name, receive the HTML, discover and fetch the render-blocking CSS, then paint.",
            "No HTML can arrive before the browser has an IP to connect to — DNS strictly precedes the response.",
            "Painting before a *render-blocking* stylesheet arrives is exactly what 'render-blocking' rules out; the browser would risk a flash of unstyled content, so it waits."
          ],
          "interviewTip": "Use the phrase 'render-blocking' deliberately and note the browser's preload scanner can *start* the CSS fetch early during parsing — but paint still waits on it. That nuance shows you know the pipeline, not just the flashcard order."
        },
        {
          "id": "f6-d",
          "question": "A user's first-ever visit to your site takes about 3 seconds, but clicking through to a second page moments later takes about 300 ms. Which pair of mechanisms best explains most of the reclaimed time?",
          "options": [
            "The server simply renders faster once it recognizes the returning browser's user-agent string",
            "The second page just contains far less HTML, and raw HTML byte size dominates the total load time",
            "DNS is already cached and the TCP+TLS connection is reused, while shared assets come from the HTTP cache",
            "After the first page the browser drops HTTP and speaks raw TCP to the server, skipping protocol overhead"
          ],
          "correctAnswerIndex": 2,
          "rationale": "The first visit pays every setup cost: DNS resolution, TCP handshake, TLS negotiation, and a cold download of every asset. The second navigation skips nearly all of it — the OS/resolver cache answers DNS instantly, the still-warm connection is reused, and the stylesheet, scripts, and fonts shared across pages come from the local HTTP cache. Only the new page's HTML (and any unique assets) touches the network.",
          "optionExplanations": [
            "Servers don't get faster by recognizing a user agent; the savings are on the client and connection side, not in server rendering speed.",
            "HTML is typically a small slice of total transfer; the dominant first-visit costs are connection setup and the cold asset downloads that the second navigation avoids.",
            "Correct — cached DNS, a reused TCP+TLS connection, and HTTP-cached shared assets eliminate the setup and re-download costs that dominated the first load.",
            "HTTP is never abandoned for raw TCP; subsequent requests still speak HTTP — they're just carried over an already-established, already-encrypted connection."
          ],
          "interviewTip": "Enumerate the caches by layer — DNS cache, warm connection, HTTP asset cache — and you've implicitly answered the follow-up 'so what would you optimize for first-time visitors?' (fewer round trips, preconnect, caching headers)."
        }
      ]
    }
  ]
}
